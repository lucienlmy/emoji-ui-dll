# DLL Releases

`emoji_window_x86.dll` is the 32-bit Release build.

`emoji_window_x64.dll` is the 64-bit Release build.

Run `.\build_release_dlls.ps1` from the repository root to rebuild both files.
