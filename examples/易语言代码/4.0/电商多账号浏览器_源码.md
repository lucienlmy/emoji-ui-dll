.版本 2
.支持库 spec

.程序集 程序集1

.子程序 _启动子程序, 整数型
.局部变量 标题字节集, 字节集
.局部变量 图标字节集, 字节集

窗口宽度 ＝ #窗口默认宽度
窗口高度 ＝ #窗口默认高度
是否深色主题 ＝ 假
当前环境节点ID ＝ 0

标题字节集 ＝ 编码_Ansi到Utf8 (“电商多账号浏览器 - 草图版”)
主窗口 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), -1, -1, 窗口宽度, 窗口高度, 到ARGB (255, 37, 99, 235), 到ARGB (255, 244, 247, 251))
.如果真 (主窗口 ＝ 0)
    信息框 (“创建主窗口失败”, 0, , )
    返回 (0)
.如果真结束

设置标题栏文字颜色 (主窗口, 到ARGB (255, 255, 255, 255))

.如果真 (文件是否存在 (取运行目录 () ＋ “\favicon.ico”))
    图标字节集 ＝ 读入文件 (取运行目录 () ＋ “\favicon.ico”)
    设置窗口图标_字节集 (主窗口, 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
.如果真结束

创建界面 ()
构建树形菜单 ()
刷新环境页 (“美区-环境01”, “Amazon 店群”, “amazon.com”, “US-Proxy-01”, “运行中”, 92)

设置按钮点击回调 (&按钮点击回调)
设置树形框回调 (树形框, #回调_树节点选中, &树形框选中回调)
设置消息循环主窗口 (主窗口)
显示隐藏窗口 (主窗口, 真)
运行消息循环 ()
返回 (0)

.子程序 创建界面
.局部变量 左侧X, 整数型
.局部变量 顶部Y, 整数型
.局部变量 右侧X, 整数型
.局部变量 右侧宽, 整数型
.局部变量 工具栏Y, 整数型
.局部变量 浏览器Y, 整数型

左侧X ＝ #外边距
顶部Y ＝ #标题栏高度 ＋ #内容顶部偏移
右侧X ＝ 左侧X ＋ #左侧宽度 ＋ #区块间距
右侧宽 ＝ 窗口宽度 － 右侧X － #外边距
工具栏Y ＝ 顶部Y ＋ #信息栏高度 ＋ 30
浏览器Y ＝ 工具栏Y ＋ #工具栏高度 ＋ 10

标签_左侧标题 ＝ 创建标签_文本 (主窗口, “环境面板”, 左侧X ＋ 14, 顶部Y ＋ 0, 140, 20, 12, 真, 到ARGB (255, 31, 41, 55), 到ARGB (255, 244, 247, 251))
标签_信息主标题 ＝ 创建标签_文本 (主窗口, “当前环境：美区-环境01   分组：Amazon 店群   状态：运行中   评分：92分”, 右侧X ＋ 16, 顶部Y ＋ 10, 860, 24, 13, 真, 到ARGB (255, 31, 41, 55), 到ARGB (255, 255, 255, 255))
标签_信息副标题 ＝ 创建标签_文本 (主窗口, “域名：amazon.com   代理：US-Proxy-01”, 右侧X ＋ 16, 顶部Y ＋ 34, 640, 20, 11, 假, 到ARGB (255, 100, 116, 139), 到ARGB (255, 255, 255, 255))
标签_浏览器标题 ＝ 创建标签_文本 (主窗口, “浏览器主工作区”, 右侧X ＋ 18, 浏览器Y ＋ 18, 360, 26, 18, 真, 到ARGB (255, 31, 41, 55), 到ARGB (255, 244, 247, 251))
标签_浏览器说明 ＝ 创建标签_文本 (主窗口, “当前环境的浏览器区域占位壳子”, 右侧X ＋ 18, 浏览器Y ＋ 46, 420, 20, 11, 假, 到ARGB (255, 100, 116, 139), 到ARGB (255, 244, 247, 251))
标签_浏览器占位 ＝ 创建标签_文本 (主窗口, “美区-环境01”, 右侧X ＋ 260, 浏览器Y ＋ 220, 300, 34, 18, 真, 到ARGB (255, 37, 99, 235), 到ARGB (255, 244, 247, 251))

按钮_新建环境 ＝ 创建按钮_文本 (主窗口, “新建环境”, 左侧X ＋ 14, 顶部Y ＋ 18, 102, 30, 到ARGB (255, 37, 99, 235))
按钮_删除环境 ＝ 创建按钮_文本 (主窗口, “删除环境”, 左侧X ＋ 126, 顶部Y ＋ 18, 102, 30, 到ARGB (255, 239, 68, 68))
按钮_换肤 ＝ 创建按钮_文本 (主窗口, “主题”, 左侧X ＋ 264, 顶部Y ＋ 18, 42, 30, 到ARGB (255, 245, 158, 11))

按钮_启动浏览器 ＝ 创建按钮_文本 (主窗口, “启动浏览器”, 右侧X ＋ 16, 工具栏Y ＋ 6, 92, 34, 到ARGB (255, 34, 197, 94))
按钮_停止 ＝ 创建按钮_文本 (主窗口, “停止”, 右侧X ＋ 116, 工具栏Y ＋ 6, 72, 34, 到ARGB (255, 245, 158, 11))
按钮_刷新 ＝ 创建按钮_文本 (主窗口, “刷新”, 右侧X ＋ 196, 工具栏Y ＋ 6, 72, 34, 到ARGB (255, 59, 130, 246))
按钮_打开后台 ＝ 创建按钮_文本 (主窗口, “打开后台”, 右侧X ＋ 276, 工具栏Y ＋ 6, 86, 34, 到ARGB (255, 15, 118, 110))
按钮_同步Cookie ＝ 创建按钮_文本 (主窗口, “同步Cookie”, 右侧X ＋ 370, 工具栏Y ＋ 6, 96, 34, 到ARGB (255, 124, 58, 237))
按钮_切换代理 ＝ 创建按钮_文本 (主窗口, “切换代理”, 右侧X ＋ 474, 工具栏Y ＋ 6, 88, 34, 到ARGB (255, 8, 145, 178))
按钮_更多操作 ＝ 创建按钮_文本 (主窗口, “更多操作”, 右侧X ＋ 570, 工具栏Y ＋ 6, 88, 34, 到ARGB (255, 100, 116, 139))

树形框 ＝ 创建树形框 (主窗口, 左侧X ＋ 12, 顶部Y ＋ #左侧顶部高度 ＋ 12, #左侧宽度 － 24, 窗口高度 － 顶部Y － #左侧顶部高度 － #外边距, 4294967295, 4281348403, 0)
.如果真 (树形框 ＝ 0)
    信息框 (“创建树形框失败”, 0, , )
    返回 ()
.如果真结束

ShowWindow (树形框, 5)
清空树形框 (树形框)
设置树形框侧栏模式 (树形框, 真)
设置树形框行高 (树形框, 38)
设置树形框行间距 (树形框, 2)
设置树形框文字色 (树形框, 4281348403)
设置树形框选中背景色 (树形框, 859870950)
设置树形框选中前景色 (树形框, 4282423014)
设置树形框悬停背景色 (树形框, 4294309882)
设置树形框字体_文本 (树形框, “Segoe UI Emoji”, 14, 400, 假)

显示标签 (标签_左侧标题, 真)
显示标签 (标签_信息主标题, 真)
显示标签 (标签_信息副标题, 真)
显示标签 (标签_浏览器标题, 真)
显示标签 (标签_浏览器说明, 真)
显示标签 (标签_浏览器占位, 真)
显示隐藏按钮 (按钮_新建环境, 真)
显示隐藏按钮 (按钮_删除环境, 真)
显示隐藏按钮 (按钮_换肤, 真)
显示隐藏按钮 (按钮_启动浏览器, 真)
显示隐藏按钮 (按钮_停止, 真)
显示隐藏按钮 (按钮_刷新, 真)
显示隐藏按钮 (按钮_打开后台, 真)
显示隐藏按钮 (按钮_同步Cookie, 真)
显示隐藏按钮 (按钮_切换代理, 真)
显示隐藏按钮 (按钮_更多操作, 真)

.子程序 创建标签_文本, 整数型
.参数 父窗口, 整数型
.参数 文本内容, 文本型
.参数 X坐标, 整数型
.参数 Y坐标, 整数型
.参数 宽度, 整数型
.参数 高度, 整数型
.参数 字号, 整数型
.参数 粗体, 逻辑型
.参数 前景色, 整数型
.参数 背景色, 整数型
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (文本内容)
字体字节集 ＝ 编码_Ansi到Utf8 (“Microsoft YaHei UI”)
返回 (创建标签 (父窗口, X坐标, Y坐标, 宽度, 高度, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 前景色, 背景色, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 字号, 粗体, 假, 假, 0, 真))

.子程序 创建按钮_文本, 整数型
.参数 父窗口, 整数型
.参数 文本内容, 文本型
.参数 X坐标, 整数型
.参数 Y坐标, 整数型
.参数 宽度, 整数型
.参数 高度, 整数型
.参数 背景色, 整数型
.局部变量 空Emoji, 字节集
.局部变量 文本字节集, 字节集

空Emoji ＝ 编码_Ansi到Utf8 (“”)
文本字节集 ＝ 编码_Ansi到Utf8 (文本内容)
返回 (创建Emoji按钮_字节集 (父窗口, 取变量数据地址 (空Emoji), 取字节集长度 (空Emoji), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), X坐标, Y坐标, 宽度, 高度, 背景色))

.子程序 设置树形框字体_文本
.参数 树句柄, 整数型
.参数 字体名, 文本型
.参数 字号, 小数型
.参数 字重, 整数型
.参数 斜体, 逻辑型
.局部变量 字体字节集, 字节集

字体字节集 ＝ 编码_Ansi到Utf8 (字体名)
设置树形框字体 (树句柄, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 字号, 字重, 斜体)

.子程序 取标签文本_文本, 文本型
.参数 标签句柄, 整数型
.局部变量 文本长度, 整数型
.局部变量 缓冲区, 字节集

文本长度 ＝ 获取标签文本 (标签句柄, 0, 0)
.如果真 (文本长度 ＞ 0)
    缓冲区 ＝ 取空白字节集 (文本长度)
    获取标签文本 (标签句柄, 取变量数据地址 (缓冲区), 文本长度)
    返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 文本长度)))
.如果真结束

返回 (“”)

.子程序 重建标签控件
.局部变量 左侧标题文本, 文本型
.局部变量 信息主标题文本, 文本型
.局部变量 信息副标题文本, 文本型
.局部变量 浏览器标题文本, 文本型
.局部变量 浏览器说明文本, 文本型
.局部变量 浏览器占位文本, 文本型
.局部变量 左侧X, 整数型
.局部变量 顶部Y, 整数型
.局部变量 右侧X, 整数型
.局部变量 右侧宽, 整数型
.局部变量 工具栏Y, 整数型
.局部变量 浏览器Y, 整数型

左侧标题文本 ＝ 取标签文本_文本 (标签_左侧标题)
信息主标题文本 ＝ 取标签文本_文本 (标签_信息主标题)
信息副标题文本 ＝ 取标签文本_文本 (标签_信息副标题)
浏览器标题文本 ＝ 取标签文本_文本 (标签_浏览器标题)
浏览器说明文本 ＝ 取标签文本_文本 (标签_浏览器说明)
浏览器占位文本 ＝ 取标签文本_文本 (标签_浏览器占位)

.如果真 (标签_左侧标题 ≠ 0)
    销毁窗口 (标签_左侧标题)
.如果真结束
.如果真 (标签_信息主标题 ≠ 0)
    销毁窗口 (标签_信息主标题)
.如果真结束
.如果真 (标签_信息副标题 ≠ 0)
    销毁窗口 (标签_信息副标题)
.如果真结束
.如果真 (标签_浏览器标题 ≠ 0)
    销毁窗口 (标签_浏览器标题)
.如果真结束
.如果真 (标签_浏览器说明 ≠ 0)
    销毁窗口 (标签_浏览器说明)
.如果真结束
.如果真 (标签_浏览器占位 ≠ 0)
    销毁窗口 (标签_浏览器占位)
.如果真结束

左侧X ＝ #外边距
顶部Y ＝ #标题栏高度 ＋ #内容顶部偏移
右侧X ＝ 左侧X ＋ #左侧宽度 ＋ #区块间距
右侧宽 ＝ 窗口宽度 － 右侧X － #外边距
工具栏Y ＝ 顶部Y ＋ #信息栏高度 ＋ 30
浏览器Y ＝ 工具栏Y ＋ #工具栏高度 ＋ 10

.如果 (是否深色主题 ＝ 假)
    标签_左侧标题 ＝ 创建标签_文本 (主窗口, 左侧标题文本, 左侧X ＋ 14, 顶部Y ＋ 0, 140, 20, 12, 真, 到ARGB (255, 31, 41, 55), 到ARGB (255, 244, 247, 251))
    标签_信息主标题 ＝ 创建标签_文本 (主窗口, 信息主标题文本, 右侧X ＋ 16, 顶部Y ＋ 10, 860, 24, 13, 真, 到ARGB (255, 31, 41, 55), 到ARGB (255, 255, 255, 255))
    标签_信息副标题 ＝ 创建标签_文本 (主窗口, 信息副标题文本, 右侧X ＋ 16, 顶部Y ＋ 34, 640, 20, 11, 假, 到ARGB (255, 100, 116, 139), 到ARGB (255, 255, 255, 255))
    标签_浏览器标题 ＝ 创建标签_文本 (主窗口, 浏览器标题文本, 右侧X ＋ 18, 浏览器Y ＋ 18, 360, 26, 18, 真, 到ARGB (255, 31, 41, 55), 到ARGB (255, 244, 247, 251))
    标签_浏览器说明 ＝ 创建标签_文本 (主窗口, 浏览器说明文本, 右侧X ＋ 18, 浏览器Y ＋ 46, 420, 20, 11, 假, 到ARGB (255, 100, 116, 139), 到ARGB (255, 244, 247, 251))
    标签_浏览器占位 ＝ 创建标签_文本 (主窗口, 浏览器占位文本, 右侧X ＋ 260, 浏览器Y ＋ 220, 300, 34, 18, 真, 到ARGB (255, 37, 99, 235), 到ARGB (255, 244, 247, 251))
.否则
    标签_左侧标题 ＝ 创建标签_文本 (主窗口, 左侧标题文本, 左侧X ＋ 14, 顶部Y ＋ 0, 140, 20, 12, 真, 到ARGB (255, 226, 232, 240), 到ARGB (255, 20, 23, 28))
    标签_信息主标题 ＝ 创建标签_文本 (主窗口, 信息主标题文本, 右侧X ＋ 16, 顶部Y ＋ 10, 860, 24, 13, 真, 到ARGB (255, 226, 232, 240), 到ARGB (255, 20, 23, 28))
    标签_信息副标题 ＝ 创建标签_文本 (主窗口, 信息副标题文本, 右侧X ＋ 16, 顶部Y ＋ 34, 640, 20, 11, 假, 到ARGB (255, 148, 163, 184), 到ARGB (255, 20, 23, 28))
    标签_浏览器标题 ＝ 创建标签_文本 (主窗口, 浏览器标题文本, 右侧X ＋ 18, 浏览器Y ＋ 18, 360, 26, 18, 真, 到ARGB (255, 226, 232, 240), 到ARGB (255, 20, 23, 28))
    标签_浏览器说明 ＝ 创建标签_文本 (主窗口, 浏览器说明文本, 右侧X ＋ 18, 浏览器Y ＋ 46, 420, 20, 11, 假, 到ARGB (255, 148, 163, 184), 到ARGB (255, 20, 23, 28))
    标签_浏览器占位 ＝ 创建标签_文本 (主窗口, 浏览器占位文本, 右侧X ＋ 260, 浏览器Y ＋ 220, 300, 34, 18, 真, 到ARGB (255, 96, 165, 250), 到ARGB (255, 20, 23, 28))
.如果结束

显示标签 (标签_左侧标题, 真)
显示标签 (标签_信息主标题, 真)
显示标签 (标签_信息副标题, 真)
显示标签 (标签_浏览器标题, 真)
显示标签 (标签_浏览器说明, 真)
显示标签 (标签_浏览器占位, 真)

.子程序 重建树形框控件
.局部变量 左侧X, 整数型
.局部变量 顶部Y, 整数型
.局部变量 左侧高, 整数型
.局部变量 当前环境键, 整数型

当前环境键 ＝ 0
.如果真 (当前环境节点ID ＝ 节点_美区环境01)
    当前环境键 ＝ 1
.如果真结束
.如果真 (当前环境节点ID ＝ 节点_美区环境02)
    当前环境键 ＝ 2
.如果真结束
.如果真 (当前环境节点ID ＝ 节点_TK环境01)
    当前环境键 ＝ 3
.如果真结束
.如果真 (当前环境节点ID ＝ 节点_独立站环境01)
    当前环境键 ＝ 4
.如果真结束

.如果真 (树形框 ≠ 0)
    销毁树形框 (树形框)
.如果真结束

左侧X ＝ #外边距
顶部Y ＝ #标题栏高度 ＋ #内容顶部偏移
左侧高 ＝ 窗口高度 － 顶部Y － #外边距

.如果 (是否深色主题 ＝ 假)
    树形框 ＝ 创建树形框 (主窗口, 左侧X ＋ 12, 顶部Y ＋ #左侧顶部高度 ＋ 12, #左侧宽度 － 24, 窗口高度 － 顶部Y － #左侧顶部高度 － #外边距, 4294967295, 4281348403, 0)
.否则
    树形框 ＝ 创建树形框 (主窗口, 左侧X ＋ 12, 顶部Y ＋ #左侧顶部高度 ＋ 12, #左侧宽度 － 24, 窗口高度 － 顶部Y － #左侧顶部高度 － #外边距, 到ARGB (255, 24, 28, 36), 到ARGB (255, 226, 232, 240), 0)
.如果结束

.如果真 (树形框 ＝ 0)
    信息框 (“重建树形框失败”, 0, , )
    返回 ()
.如果真结束

ShowWindow (树形框, 5)
清空树形框 (树形框)
设置树形框侧栏模式 (树形框, 真)
设置树形框行高 (树形框, 38)
设置树形框行间距 (树形框, 2)

.如果 (是否深色主题 ＝ 假)
    设置树形框文字色 (树形框, 4281348403)
    设置树形框选中背景色 (树形框, 859870950)
    设置树形框选中前景色 (树形框, 4282423014)
    设置树形框悬停背景色 (树形框, 4294309882)
.否则
    设置树形框文字色 (树形框, 到ARGB (255, 226, 232, 240))
    设置树形框选中背景色 (树形框, 到ARGB (255, 37, 99, 235))
    设置树形框选中前景色 (树形框, 到ARGB (255, 255, 255, 255))
    设置树形框悬停背景色 (树形框, 到ARGB (255, 45, 55, 72))
.如果结束

设置树形框字体_文本 (树形框, “Segoe UI Emoji”, 14, 400, 假)
构建树形菜单 ()
设置树形框回调 (树形框, #回调_树节点选中, &树形框选中回调)

.如果 (当前环境键 ＝ 1)
    当前环境节点ID ＝ 节点_美区环境01
    设置选中树节点 (树形框, 节点_美区环境01)
.否则
    .如果 (当前环境键 ＝ 2)
        当前环境节点ID ＝ 节点_美区环境02
        设置选中树节点 (树形框, 节点_美区环境02)
    .否则
        .如果 (当前环境键 ＝ 3)
            当前环境节点ID ＝ 节点_TK环境01
            设置选中树节点 (树形框, 节点_TK环境01)
        .否则
            .如果 (当前环境键 ＝ 4)
                当前环境节点ID ＝ 节点_独立站环境01
                设置选中树节点 (树形框, 节点_独立站环境01)
            .如果结束
        .如果结束
    .如果结束
.如果结束

.子程序 构建树形菜单
.局部变量 文本, 字节集
.局部变量 图标, 字节集

文本 ＝ 编码_Ansi到Utf8 (“环境切换”)
图标 ＝ { 240, 159, 167, 173 }
节点_环境切换 ＝ 添加根节点 (树形框, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))

文本 ＝ 编码_Ansi到Utf8 (“Amazon 店群”)
图标 ＝ { 240, 159, 147, 129 }
节点_Amazon组 ＝ 添加子节点 (树形框, 节点_环境切换, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“美区-环境01”)
节点_美区环境01 ＝ 添加子节点 (树形框, 节点_Amazon组, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)
文本 ＝ 编码_Ansi到Utf8 (“美区-环境02”)
节点_美区环境02 ＝ 添加子节点 (树形框, 节点_Amazon组, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

文本 ＝ 编码_Ansi到Utf8 (“TikTok 店群”)
图标 ＝ { 240, 159, 147, 129 }
节点_TikTok组 ＝ 添加子节点 (树形框, 节点_环境切换, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“TK-环境01”)
节点_TK环境01 ＝ 添加子节点 (树形框, 节点_TikTok组, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

文本 ＝ 编码_Ansi到Utf8 (“独立站”)
图标 ＝ { 240, 159, 147, 129 }
节点_独立站组 ＝ 添加子节点 (树形框, 节点_环境切换, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“独立站-环境01”)
节点_独立站环境01 ＝ 添加子节点 (树形框, 节点_独立站组, 取变量数据地址 (文本), 取字节集长度 (文本), 0, 0)

文本 ＝ 编码_Ansi到Utf8 (“代理管理”)
图标 ＝ { 240, 159, 140, 144 }
节点_代理管理 ＝ 添加根节点 (树形框, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“分组管理”)
图标 ＝ { 240, 159, 151, 130 }
节点_分组管理 ＝ 添加根节点 (树形框, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“RPA自动化”)
图标 ＝ { 240, 159, 164, 150 }
节点_RPA自动化 ＝ 添加根节点 (树形框, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“插件中心”)
图标 ＝ { 240, 159, 167, 169 }
节点_插件中心 ＝ 添加根节点 (树形框, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“团队协作”)
图标 ＝ { 240, 159, 145, 165 }
节点_团队协作 ＝ 添加根节点 (树形框, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))
文本 ＝ 编码_Ansi到Utf8 (“系统设置”)
图标 ＝ { 226, 154, 153, 239, 184, 143 }
节点_系统设置 ＝ 添加根节点 (树形框, 取变量数据地址 (文本), 取字节集长度 (文本), 取变量数据地址 (图标), 取字节集长度 (图标))

.如果真 (节点_环境切换 ＝ 0)
    信息框 (“树形框根节点创建失败”, 0, , )
    返回 ()
.如果真结束

展开全部树节点 (树形框)
设置选中树节点 (树形框, 节点_环境切换)

.子程序 应用主题
.局部变量 文本字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (“主题”)

.如果 (是否深色主题 ＝ 假)
    设置窗口标题栏颜色 (主窗口, 到ARGB (255, 37, 99, 235))
    设置窗口背景色 (主窗口, 到ARGB (255, 244, 247, 251))
    设置按钮文本 (按钮_换肤, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.否则
    设置窗口标题栏颜色 (主窗口, 到ARGB (255, 15, 23, 42))
    设置窗口背景色 (主窗口, 到ARGB (255, 20, 23, 28))
    设置按钮文本 (按钮_换肤, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.如果结束

重建标签控件 ()
重建树形框控件 ()


.子程序 刷新默认环境

当前环境节点ID ＝ 节点_美区环境01
设置选中树节点 (树形框, 节点_美区环境01)
刷新环境页 (“美区-环境01”, “Amazon 店群”, “amazon.com”, “US-Proxy-01”, “运行中”, 92)

.子程序 刷新环境页
.参数 环境名, 文本型
.参数 分组名, 文本型
.参数 域名, 文本型
.参数 代理名, 文本型
.参数 状态, 文本型
.参数 评分, 整数型

工具栏显示 ＝ 真
设置标签文本_文本 (标签_信息主标题, “当前环境：” ＋ 环境名 ＋ “   分组：” ＋ 分组名 ＋ “   状态：” ＋ 状态 ＋ “   评分：” ＋ 到文本 (评分) ＋ “分”)
设置标签文本_文本 (标签_信息副标题, “域名：” ＋ 域名 ＋ “   代理：” ＋ 代理名)
设置标签文本_文本 (标签_浏览器占位, 环境名)

.子程序 刷新模块页
.参数 模块名, 文本型

工具栏显示 ＝ 假
设置标签文本_文本 (标签_信息主标题, “当前模块：” ＋ 模块名)
设置标签文本_文本 (标签_信息副标题, “该区域先保留为模块工作区占位”)
设置标签文本_文本 (标签_浏览器占位, 模块名)

.子程序 重新布局
.局部变量 左侧X, 整数型
.局部变量 顶部Y, 整数型
.局部变量 左侧高, 整数型
.局部变量 右侧X, 整数型
.局部变量 右侧宽, 整数型
.局部变量 右侧高, 整数型
.局部变量 工具栏Y, 整数型
.局部变量 浏览器Y, 整数型
.局部变量 浏览器高, 整数型

左侧X ＝ #外边距
顶部Y ＝ #标题栏高度 ＋ #内容顶部偏移
左侧高 ＝ 窗口高度 － 顶部Y － #外边距
右侧X ＝ 左侧X ＋ #左侧宽度 ＋ #区块间距
右侧宽 ＝ 窗口宽度 － 右侧X － #外边距
右侧高 ＝ 左侧高
工具栏Y ＝ 顶部Y ＋ #信息栏高度 ＋ 60
浏览器Y ＝ 工具栏Y ＋ #工具栏高度 ＋ 10
浏览器高 ＝ 右侧高 － #信息栏高度 － #工具栏高度 － 20

MoveWindow (左侧背景, 左侧X, 顶部Y, #左侧宽度, 左侧高, 真)
MoveWindow (右侧信息栏背景, 右侧X, 顶部Y, 右侧宽, #信息栏高度, 真)
MoveWindow (右侧工具栏背景, 右侧X, 工具栏Y, 右侧宽, #工具栏高度, 真)
MoveWindow (右侧浏览器背景, 右侧X, 浏览器Y, 右侧宽, 浏览器高, 真)

设置标签位置 (标签_左侧标题, 左侧X ＋ 14, 顶部Y ＋ 4, 140, 20)
设置标签位置 (标签_信息主标题, 右侧X ＋ 16, 顶部Y ＋ 10, 860, 24)
设置标签位置 (标签_信息副标题, 右侧X ＋ 16, 顶部Y ＋ 34, 640, 20)
设置标签位置 (标签_浏览器标题, 右侧X ＋ 18, 浏览器Y ＋ 18, 360, 26)
设置标签位置 (标签_浏览器说明, 右侧X ＋ 18, 浏览器Y ＋ 46, 420, 20)
设置标签位置 (标签_浏览器占位, 右侧X ＋ 260, 浏览器Y ＋ 220, 300, 34)

设置按钮位置 (按钮_新建环境, 左侧X ＋ 14, 顶部Y ＋ 48, 102, 30)
设置按钮位置 (按钮_删除环境, 左侧X ＋ 126, 顶部Y ＋ 48, 102, 30)
设置按钮位置 (按钮_换肤, 左侧X ＋ 264, 顶部Y ＋ 48, 42, 30)

MoveWindow (树形框, 左侧X ＋ 12, 顶部Y ＋ #左侧顶部高度 ＋ 42, #左侧宽度 － 24, 左侧高 － #左侧顶部高度 － 16, 真)
ShowWindow (树形框, 5)

.如果 (工具栏显示)
    显示隐藏按钮 (按钮_启动浏览器, 真)
    显示隐藏按钮 (按钮_停止, 真)
    显示隐藏按钮 (按钮_刷新, 真)
    显示隐藏按钮 (按钮_打开后台, 真)
    显示隐藏按钮 (按钮_同步Cookie, 真)
    显示隐藏按钮 (按钮_切换代理, 真)
    显示隐藏按钮 (按钮_更多操作, 真)
    设置按钮位置 (按钮_启动浏览器, 右侧X ＋ 16, 工具栏Y ＋ 6, 92, 34)
    设置按钮位置 (按钮_停止, 右侧X ＋ 116, 工具栏Y ＋ 6, 72, 34)
    设置按钮位置 (按钮_刷新, 右侧X ＋ 196, 工具栏Y ＋ 6, 72, 34)
    设置按钮位置 (按钮_打开后台, 右侧X ＋ 276, 工具栏Y ＋ 6, 86, 34)
    设置按钮位置 (按钮_同步Cookie, 右侧X ＋ 370, 工具栏Y ＋ 6, 96, 34)
    设置按钮位置 (按钮_切换代理, 右侧X ＋ 474, 工具栏Y ＋ 6, 88, 34)
    设置按钮位置 (按钮_更多操作, 右侧X ＋ 570, 工具栏Y ＋ 6, 88, 34)
.否则
    显示隐藏按钮 (按钮_启动浏览器, 假)
    显示隐藏按钮 (按钮_停止, 假)
    显示隐藏按钮 (按钮_刷新, 假)
    显示隐藏按钮 (按钮_打开后台, 假)
    显示隐藏按钮 (按钮_同步Cookie, 假)
    显示隐藏按钮 (按钮_切换代理, 假)
    显示隐藏按钮 (按钮_更多操作, 假)
.如果结束


.子程序 树形框选中回调, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

.判断开始 (节点ID ＝ 节点_美区环境01)
    当前环境节点ID ＝ 节点ID
    刷新环境页 (“美区-环境01”, “Amazon 店群”, “amazon.com”, “US-Proxy-01”, “运行中”, 92)
.判断 (节点ID ＝ 节点_美区环境02)
    当前环境节点ID ＝ 节点ID
    刷新环境页 (“美区-环境02”, “Amazon 店群”, “amazon.com”, “US-Proxy-02”, “空闲中”, 88)
.判断 (节点ID ＝ 节点_TK环境01)
    当前环境节点ID ＝ 节点ID
    刷新环境页 (“TK-环境01”, “TikTok 店群”, “seller-us.tiktok.com”, “US-Proxy-09”, “运行中”, 95)
.判断 (节点ID ＝ 节点_独立站环境01)
    当前环境节点ID ＝ 节点ID
    刷新环境页 (“独立站-环境01”, “独立站”, “shop.example.com”, “JP-Proxy-03”, “待启动”, 84)
.判断 (节点ID ＝ 节点_Amazon组)
    刷新模块页 (“Amazon 店群”)
.判断 (节点ID ＝ 节点_TikTok组)
    刷新模块页 (“TikTok 店群”)
.判断 (节点ID ＝ 节点_独立站组)
    刷新模块页 (“独立站”)
.判断 (节点ID ＝ 节点_代理管理)
    刷新模块页 (“代理管理”)
.判断 (节点ID ＝ 节点_分组管理)
    刷新模块页 (“分组管理”)
.判断 (节点ID ＝ 节点_RPA自动化)
    刷新模块页 (“RPA自动化”)
.判断 (节点ID ＝ 节点_插件中心)
    刷新模块页 (“插件中心”)
.判断 (节点ID ＝ 节点_团队协作)
    刷新模块页 (“团队协作”)
.判断 (节点ID ＝ 节点_系统设置)
    刷新模块页 (“系统设置”)
.默认
    刷新模块页 (“环境切换”)
.判断结束

重新布局 ()

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 父窗口, 整数型

.判断开始 (按钮ID ＝ 按钮_新建环境)

.判断 (按钮ID ＝ 按钮_删除环境)

.判断 (按钮ID ＝ 按钮_换肤)
    是否深色主题 ＝ 取反 (是否深色主题)
    应用主题 ()
.判断 (按钮ID ＝ 按钮_启动浏览器)
    设置标签文本_文本 (标签_浏览器说明, “已触发当前环境的草图操作，占位逻辑生效。”)
.默认

.判断结束

重新布局 ()

.子程序 设置标签文本_文本
.参数 标签句柄, 整数型
.参数 文本内容, 文本型
.局部变量 文本字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (文本内容)
设置标签文本 (标签句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
