# 电商多账号浏览器草图版 完整源码稿

说明：

- 这份文档按“窗口程序集源码稿”风格整理
- 目标是尽量接近当前 Python 版本：
  - `examples/Python/电商多账号浏览器/demo_env_workspace_sketch.py`
- 你需要做的主要是：
  - 把 DLL 命令名替换成你本地模块实际导出的名称
  - 把颜色辅助函数、编码辅助函数按你现有支持方式接起来

建议：

- 先看 `电商多账号浏览器_草图版_可直接粘贴骨架.md`
- 再把下面这份整段源码稿整体复制进 IDE 分段整理

---

## 一、程序集变量与常量

```text
.版本 2

.程序集变量 主窗口, 整数型
.程序集变量 左侧面板, 整数型
.程序集变量 左侧顶部面板, 整数型
.程序集变量 左侧树形区面板, 整数型
.程序集变量 右侧面板, 整数型
.程序集变量 信息栏面板, 整数型
.程序集变量 工具栏面板, 整数型
.程序集变量 浏览器面板, 整数型
.程序集变量 浏览器外框, 整数型
.程序集变量 浏览器画布, 整数型
.程序集变量 树形框, 整数型

.程序集变量 标签_左侧标题, 整数型
.程序集变量 标签_信息主标题, 整数型
.程序集变量 标签_信息副标题, 整数型
.程序集变量 标签_浏览器标题, 整数型
.程序集变量 标签_浏览器说明, 整数型
.程序集变量 标签_浏览器占位, 整数型

.程序集变量 按钮_新建环境, 整数型
.程序集变量 按钮_删除环境, 整数型
.程序集变量 按钮_换肤, 整数型
.程序集变量 按钮_启动浏览器, 整数型
.程序集变量 按钮_停止, 整数型
.程序集变量 按钮_刷新, 整数型
.程序集变量 按钮_打开后台, 整数型
.程序集变量 按钮_同步Cookie, 整数型
.程序集变量 按钮_切换代理, 整数型
.程序集变量 按钮_更多操作, 整数型

.程序集变量 节点_环境切换, 整数型
.程序集变量 节点_Amazon组, 整数型
.程序集变量 节点_TikTok组, 整数型
.程序集变量 节点_独立站组, 整数型
.程序集变量 节点_美区环境01, 整数型
.程序集变量 节点_美区环境02, 整数型
.程序集变量 节点_TK环境01, 整数型
.程序集变量 节点_独立站环境01, 整数型
.程序集变量 节点_代理管理, 整数型
.程序集变量 节点_分组管理, 整数型
.程序集变量 节点_RPA自动化, 整数型
.程序集变量 节点_插件中心, 整数型
.程序集变量 节点_团队协作, 整数型
.程序集变量 节点_系统设置, 整数型

.程序集变量 当前选中节点ID, 整数型
.程序集变量 当前环境节点ID, 整数型
.程序集变量 是否深色主题, 逻辑型
.程序集变量 工具栏显示, 逻辑型
.程序集变量 环境计数器, 整数型
.程序集变量 窗口宽度, 整数型
.程序集变量 窗口高度, 整数型

.常量 窗口默认宽度, 1480
.常量 窗口默认高度, 920
.常量 标题栏高度, 32
.常量 内容顶部偏移, 6
.常量 外边距, 16
.常量 左侧宽度, 320
.常量 区块间距, 12
.常量 左侧顶部高度, 56
.常量 信息栏高度, 52
.常量 工具栏高度, 48
.常量 回调_树节点选中, 1

.常量 颜色_标题栏蓝, 16进制到十进制 (“FF2563EB”)
.常量 颜色_页面浅灰, 16进制到十进制 (“FFF4F7FB”)
.常量 颜色_白, 16进制到十进制 (“FFFFFFFF”)
.常量 颜色_深字, 16进制到十进制 (“FF1F2937”)
.常量 颜色_浅字, 16进制到十进制 (“FF64748B”)
.常量 颜色_危险红, 16进制到十进制 (“FFEF4444”)
.常量 颜色_按钮蓝, 16进制到十进制 (“FF2563EB”)
.常量 颜色_按钮黄, 16进制到十进制 (“FFF59E0B”)
.常量 颜色_选中蓝, 16进制到十进制 (“FF2563EB”)
```

---

## 二、启动子程序

```text
.子程序 _启动子程序, 整数型

窗口宽度 ＝ 窗口默认宽度
窗口高度 ＝ 窗口默认高度
工具栏显示 ＝ 真
是否深色主题 ＝ 假
环境计数器 ＝ 5

主窗口 ＝ 创建Emoji窗口_字节集扩展 (“电商多账号浏览器 - 草图版”, -1, -1, 窗口宽度, 窗口高度, 颜色_标题栏蓝, 颜色_页面浅灰)
设置标题栏文字颜色 (主窗口, 颜色_白)

如果 (文件是否存在 (“favicon.ico”))
    设置窗口图标_字节集 (主窗口, 读入文件 (“favicon.ico”))
如果结束

调用 创建界面
调用 构建树形菜单
调用 应用主题
调用 刷新默认环境
调用 重新布局

设置按钮点击回调 (&按钮点击回调)
设置树形框回调 (树形框, 回调_树节点选中, &树形框选中回调)
设置窗口大小改变回调 (&窗口大小改变回调)

显示窗口 (主窗口, 真)
设置消息循环主窗口 (主窗口)
运行消息循环 ()
返回 (0)
```

---

## 三、创建界面

```text
.子程序 创建界面

左侧面板 ＝ 创建面板 (主窗口, 0, 0, 100, 100, 颜色_白)
左侧顶部面板 ＝ 创建面板 (左侧面板, 0, 0, 100, 左侧顶部高度, 颜色_白)
左侧树形区面板 ＝ 创建面板 (左侧面板, 0, 0, 100, 100, 颜色_白)

右侧面板 ＝ 创建面板 (主窗口, 0, 0, 100, 100, 颜色_页面浅灰)
信息栏面板 ＝ 创建面板 (右侧面板, 0, 0, 100, 信息栏高度, 颜色_白)
工具栏面板 ＝ 创建面板 (右侧面板, 0, 0, 100, 工具栏高度, 颜色_白)
浏览器面板 ＝ 创建面板 (右侧面板, 0, 0, 100, 100, 16进制到十进制 (“FFEEF3F9”))
浏览器外框 ＝ 创建面板 (浏览器面板, 0, 0, 100, 100, 颜色_白)
浏览器画布 ＝ 创建面板 (浏览器外框, 0, 0, 100, 100, 16进制到十进制 (“FFF9FBFD”))

标签_左侧标题 ＝ 创建标签_简单 (左侧面板, “环境面板”, 12, 真)
标签_信息主标题 ＝ 创建标签_简单 (信息栏面板, “”, 13, 真)
标签_信息副标题 ＝ 创建标签_简单 (信息栏面板, “”, 11, 假)
标签_浏览器标题 ＝ 创建标签_简单 (浏览器外框, “”, 18, 真)
标签_浏览器说明 ＝ 创建标签_简单 (浏览器外框, “”, 11, 假)
标签_浏览器占位 ＝ 创建标签_简单 (浏览器画布, “”, 18, 真)

按钮_新建环境 ＝ 创建按钮_简单 (左侧面板, “新建环境”, 颜色_按钮蓝)
按钮_删除环境 ＝ 创建按钮_简单 (左侧面板, “删除环境”, 颜色_危险红)
按钮_换肤 ＝ 创建按钮_简单 (左侧面板, “🌙”, 颜色_按钮黄)

按钮_启动浏览器 ＝ 创建按钮_简单 (工具栏面板, “启动浏览器”, 16进制到十进制 (“FF22C55E”))
按钮_停止 ＝ 创建按钮_简单 (工具栏面板, “停止”, 16进制到十进制 (“FFF59E0B”))
按钮_刷新 ＝ 创建按钮_简单 (工具栏面板, “刷新”, 16进制到十进制 (“FF3B82F6”))
按钮_打开后台 ＝ 创建按钮_简单 (工具栏面板, “打开后台”, 16进制到十进制 (“FF0F766E”))
按钮_同步Cookie ＝ 创建按钮_简单 (工具栏面板, “同步Cookie”, 16进制到十进制 (“FF7C3AED”))
按钮_切换代理 ＝ 创建按钮_简单 (工具栏面板, “切换代理”, 16进制到十进制 (“FF0891B2”))
按钮_更多操作 ＝ 创建按钮_简单 (工具栏面板, “更多操作”, 16进制到十进制 (“FF64748B”))

树形框 ＝ 创建树形框 (左侧树形区面板, 0, 0, 100, 100, 颜色_白, 颜色_深字, 0)
清空树形框 (树形框)
设置树形框侧栏模式 (树形框, 真)
设置树形框行高 (树形框, 34.0)
设置树形框项目间距 (树形框, 4.0)
设置树形框字体 (树形框, “Microsoft YaHei UI”, 12.0, 400, 假)
```

---

## 四、辅助子程序

```text
.子程序 创建标签_简单, 整数型
.参数 父窗口, 整数型
.参数 文本, 文本型
.参数 字号, 整数型
.参数 粗体, 逻辑型

.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集

文本字节集 ＝ 到字节集 (文本, #编码_UTF8)
字体字节集 ＝ 到字节集 (“Microsoft YaHei UI”, #编码_UTF8)

返回 (创建标签 (父窗口, 0, 0, 100, 20, 文本字节集, 取字节集长度 (文本字节集), 颜色_深字, 颜色_白, 字体字节集, 取字节集长度 (字体字节集), 字号, 粗体, 假, 假, 0, 假))
```

```text
.子程序 创建按钮_简单, 整数型
.参数 父窗口, 整数型
.参数 文本, 文本型
.参数 背景色, 整数型

.局部变量 空Emoji, 字节集
.局部变量 文本字节集, 字节集

空Emoji ＝ 到字节集 (“”, #编码_UTF8)
文本字节集 ＝ 到字节集 (文本, #编码_UTF8)

返回 (创建Emoji按钮_字节集 (父窗口, 空Emoji, 取字节集长度 (空Emoji), 文本字节集, 取字节集长度 (文本字节集), 0, 0, 100, 34, 背景色))
```

```text
.子程序 设置标签文本_文本
.参数 标签句柄, 整数型
.参数 文本内容, 文本型

.局部变量 文本字节集, 字节集
文本字节集 ＝ 到字节集 (文本内容, #编码_UTF8)
设置标签文本 (标签句柄, 文本字节集, 取字节集长度 (文本字节集))
```

```text
.子程序 设置按钮文本_文本
.参数 按钮句柄, 整数型
.参数 文本内容, 文本型

.局部变量 文本字节集, 字节集
文本字节集 ＝ 到字节集 (文本内容, #编码_UTF8)
设置按钮文本 (按钮句柄, 文本字节集, 取字节集长度 (文本字节集))
```

```text
.子程序 设置窗口标题_文本
.参数 标题文本, 文本型

.局部变量 标题字节集, 字节集
标题字节集 ＝ 到字节集 (标题文本, #编码_UTF8)
设置窗口标题 (主窗口, 标题字节集, 取字节集长度 (标题字节集))
```

```text
.子程序 颜色加深, 整数型
.参数 ARGB颜色, 整数型
.参数 偏移值, 整数型

.局部变量 A, 整数型
.局部变量 R, 整数型
.局部变量 G, 整数型
.局部变量 B, 整数型

A ＝ 位与 (位右移 (ARGB颜色, 24), 255)
R ＝ 位与 (位右移 (ARGB颜色, 16), 255) ＋ 偏移值
G ＝ 位与 (位右移 (ARGB颜色, 8), 255) ＋ 偏移值
B ＝ 位与 (ARGB颜色, 255) ＋ 偏移值

如果 (R ＜ 0) R ＝ 0
如果 (R ＞ 255) R ＝ 255
如果 (G ＜ 0) G ＝ 0
如果 (G ＞ 255) G ＝ 255
如果 (B ＜ 0) B ＝ 0
如果 (B ＞ 255) B ＝ 255

返回 (位或 (位左移 (A, 24), 位左移 (R, 16), 位左移 (G, 8), B))
```

```text
.子程序 设置环境节点颜色
.参数 节点ID, 整数型
.参数 状态文本, 文本型

.判断开始 (状态文本)
    .如果 (状态文本 ＝ “运行中”)
        设置节点前景色 (树形框, 节点ID, 16进制到十进制 (“FF22C55E”))
    .如果 (状态文本 ＝ “空闲中”)
        设置节点前景色 (树形框, 节点ID, 16进制到十进制 (“FF64748B”))
    .如果 (状态文本 ＝ “待启动”)
        设置节点前景色 (树形框, 节点ID, 16进制到十进制 (“FFF59E0B”))
    .如果 (状态文本 ＝ “异常”)
        设置节点前景色 (树形框, 节点ID, 16进制到十进制 (“FFEF4444”))
    .默认
        设置节点前景色 (树形框, 节点ID, 颜色_深字)
.判断结束
```

---

## 五、构建树形菜单

```text
.子程序 构建树形菜单

节点_环境切换 ＝ 添加根节点 (树形框, “环境切换”, “🧭”)

节点_Amazon组 ＝ 添加子节点 (树形框, 节点_环境切换, “Amazon 店群”, “📁”)
节点_美区环境01 ＝ 添加子节点 (树形框, 节点_Amazon组, “美区-环境01”, “●”)
调用 设置环境节点颜色 (节点_美区环境01, “运行中”)
节点_美区环境02 ＝ 添加子节点 (树形框, 节点_Amazon组, “美区-环境02”, “●”)
调用 设置环境节点颜色 (节点_美区环境02, “空闲中”)

节点_TikTok组 ＝ 添加子节点 (树形框, 节点_环境切换, “TikTok 店群”, “📁”)
节点_TK环境01 ＝ 添加子节点 (树形框, 节点_TikTok组, “TK-环境01”, “●”)
调用 设置环境节点颜色 (节点_TK环境01, “运行中”)

节点_独立站组 ＝ 添加子节点 (树形框, 节点_环境切换, “独立站”, “📁”)
节点_独立站环境01 ＝ 添加子节点 (树形框, 节点_独立站组, “独立站-环境01”, “●”)
调用 设置环境节点颜色 (节点_独立站环境01, “待启动”)

节点_代理管理 ＝ 添加根节点 (树形框, “代理管理”, “🌐”)
节点_分组管理 ＝ 添加根节点 (树形框, “分组管理”, “🗂️”)
节点_RPA自动化 ＝ 添加根节点 (树形框, “RPA自动化”, “🤖”)
节点_插件中心 ＝ 添加根节点 (树形框, “插件中心”, “🧩”)
节点_团队协作 ＝ 添加根节点 (树形框, “团队协作”, “👥”)
节点_系统设置 ＝ 添加根节点 (树形框, “系统设置”, “⚙️”)

展开全部节点 (树形框)
```

---

## 六、应用主题

```text
.子程序 应用主题

.如果 (是否深色主题 ＝ 假)
    设置窗口标题栏颜色 (主窗口, 颜色_标题栏蓝)
    设置窗口背景色 (主窗口, 颜色_页面浅灰)
    设置标签颜色 (标签_左侧标题, 颜色_深字, 颜色_白)
    设置标签颜色 (标签_信息主标题, 颜色_深字, 颜色_白)
    设置标签颜色 (标签_信息副标题, 颜色_浅字, 颜色_白)
    设置标签颜色 (标签_浏览器标题, 颜色_深字, 颜色_白)
    设置标签颜色 (标签_浏览器说明, 颜色_浅字, 颜色_白)
    设置标签颜色 (标签_浏览器占位, 颜色_按钮蓝, 16进制到十进制 (“FFF9FBFD”))
    设置按钮文本_文本 (按钮_换肤, “🌙”)
.否则
    设置窗口标题栏颜色 (主窗口, 16进制到十进制 (“FF0F172A”))
    设置窗口背景色 (主窗口, 16进制到十进制 (“FF14171C”))
    设置标签颜色 (标签_左侧标题, 16进制到十进制 (“FFF1F5F9”), 16进制到十进制 (“FF1A1E24”))
    设置标签颜色 (标签_信息主标题, 16进制到十进制 (“FFF1F5F9”), 16进制到十进制 (“FF21262E”))
    设置标签颜色 (标签_信息副标题, 16进制到十进制 (“FF94A3B8”), 16进制到十进制 (“FF21262E”))
    设置标签颜色 (标签_浏览器标题, 16进制到十进制 (“FFF1F5F9”), 16进制到十进制 (“FF21262E”))
    设置标签颜色 (标签_浏览器说明, 16进制到十进制 (“FF94A3B8”), 16进制到十进制 (“FF21262E”))
    设置标签颜色 (标签_浏览器占位, 16进制到十进制 (“FF60A5FA”), 16进制到十进制 (“FF0C0F14”))
    设置按钮文本_文本 (按钮_换肤, “☀️”)
.如果结束

设置树形框背景色 (树形框, 颜色_白)
设置树形框文字色 (树形框, 颜色_深字)
设置树形框选中背景色 (树形框, 颜色_选中蓝)
设置树形框选中文字色 (树形框, 颜色_白)
设置树形框悬停背景色 (树形框, 颜色加深 (颜色_选中蓝, 10))

调用 刷新按钮样式 (按钮_新建环境, 颜色_按钮蓝, 假)
调用 刷新按钮样式 (按钮_删除环境, 颜色_危险红, 假)
调用 刷新按钮样式 (按钮_换肤, 颜色_按钮黄, 真)
调用 刷新按钮样式 (按钮_启动浏览器, 16进制到十进制 (“FF22C55E”), 真)
调用 刷新按钮样式 (按钮_停止, 16进制到十进制 (“FFF59E0B”), 真)
调用 刷新按钮样式 (按钮_刷新, 16进制到十进制 (“FF3B82F6”), 真)
调用 刷新按钮样式 (按钮_打开后台, 16进制到十进制 (“FF0F766E”), 真)
调用 刷新按钮样式 (按钮_同步Cookie, 16进制到十进制 (“FF7C3AED”), 真)
调用 刷新按钮样式 (按钮_切换代理, 16进制到十进制 (“FF0891B2”), 真)
调用 刷新按钮样式 (按钮_更多操作, 16进制到十进制 (“FF64748B”), 真)
```

```text
.子程序 刷新按钮样式
.参数 按钮句柄, 整数型
.参数 背景色, 整数型
.参数 是否圆角, 逻辑型

设置按钮样式 (按钮句柄, 0)
设置按钮尺寸 (按钮句柄, 1)
设置按钮圆角 (按钮句柄, 是否圆角)
设置按钮背景色 (按钮句柄, 背景色)
设置按钮边框色 (按钮句柄, 颜色加深 (背景色, -18))
设置按钮文字色 (按钮句柄, 颜色_白)
设置按钮悬停颜色 (按钮句柄, 颜色加深 (背景色, 10), 颜色加深 (背景色, 4), 颜色_白)
```

---

## 七、刷新默认环境与页面刷新

```text
.子程序 刷新默认环境
当前选中节点ID ＝ 节点_美区环境01
当前环境节点ID ＝ 节点_美区环境01
设置选中节点 (树形框, 节点_美区环境01)
调用 刷新环境页 (“美区-环境01”, “Amazon 店群”, “amazon.com”, “US-Proxy-01”, “运行中”, 92)
```

```text
.子程序 刷新环境页
.参数 环境名, 文本型
.参数 分组名, 文本型
.参数 域名, 文本型
.参数 代理名, 文本型
.参数 状态, 文本型
.参数 评分, 整数型

工具栏显示 ＝ 真
设置标签文本_文本 (标签_信息主标题, “当前环境：” ＋ 环境名 ＋ “   分组：” ＋ 分组名 ＋ “   状态：” ＋ 状态 ＋ “   评分：” ＋ 到文本 (评分) ＋ “分”)
设置标签文本_文本 (标签_信息副标题, “域名：” ＋ 域名 ＋ “   代理：” ＋ 代理名)
设置标签文本_文本 (标签_浏览器标题, “浏览器主工作区”)
设置标签文本_文本 (标签_浏览器说明, “当前环境 ” ＋ 环境名 ＋ “ 的浏览器区域占位壳子”)
设置标签文本_文本 (标签_浏览器占位, 环境名)
调用 设置窗口标题_文本 (“电商多账号浏览器 - ” ＋ 环境名)
```

```text
.子程序 刷新模块页
.参数 模块名, 文本型

工具栏显示 ＝ 假
设置标签文本_文本 (标签_信息主标题, “当前模块：” ＋ 模块名)
设置标签文本_文本 (标签_信息副标题, “该区域先保留为模块工作区占位，后续可替换为真实业务页面”)
设置标签文本_文本 (标签_浏览器标题, 模块名 ＋ “ 工作区”)
设置标签文本_文本 (标签_浏览器说明, “右侧区域已按 Python 草图保留为最大化内容区”)
设置标签文本_文本 (标签_浏览器占位, 模块名)
调用 设置窗口标题_文本 (“电商多账号浏览器 - ” ＋ 模块名)
```

---

## 八、布局

```text
.子程序 重新布局

.局部变量 左侧X, 整数型
.局部变量 顶部Y, 整数型
.局部变量 左侧高, 整数型
.局部变量 右侧X, 整数型
.局部变量 右侧宽, 整数型
.局部变量 右侧高, 整数型

左侧X ＝ 外边距
顶部Y ＝ 标题栏高度 ＋ 内容顶部偏移
左侧高 ＝ 取最大值 (300, 窗口高度 － 顶部Y － 外边距)
右侧X ＝ 左侧X ＋ 左侧宽度 ＋ 区块间距
右侧宽 ＝ 取最大值 (420, 窗口宽度 － 右侧X － 外边距)
右侧高 ＝ 左侧高

移动窗口 (左侧面板, 左侧X, 顶部Y, 左侧宽度, 左侧高)
移动窗口 (右侧面板, 右侧X, 顶部Y, 右侧宽, 右侧高)

移动窗口 (左侧顶部面板, 0, 0, 左侧宽度, 左侧顶部高度)
移动窗口 (左侧树形区面板, 0, 左侧顶部高度 ＋ 6, 左侧宽度, 左侧高 － 左侧顶部高度 － 6)
移动窗口 (树形框, 12, 6, 左侧宽度 － 24, 左侧高 － 左侧顶部高度 － 16)

设置标签位置 (标签_左侧标题, 14, 2, 120, 16)

设置按钮位置 (按钮_新建环境, 左侧X ＋ 14, 顶部Y ＋ 18, 102, 30)
设置按钮位置 (按钮_删除环境, 左侧X ＋ 126, 顶部Y ＋ 18, 102, 30)
设置按钮位置 (按钮_换肤, 左侧X ＋ 264, 顶部Y ＋ 18, 42, 30)

移动窗口 (信息栏面板, 0, 0, 右侧宽, 信息栏高度)
设置标签位置 (标签_信息主标题, 16, 10, 右侧宽 － 32, 18)
设置标签位置 (标签_信息副标题, 16, 28, 右侧宽 － 32, 16)

如果 (工具栏显示)
    移动窗口 (工具栏面板, 0, 信息栏高度 ＋ 10, 右侧宽, 工具栏高度)
    显示窗口 (工具栏面板, 真)
    设置按钮位置 (按钮_启动浏览器, 右侧X ＋ 16, 顶部Y ＋ 信息栏高度 ＋ 17, 92, 34)
    设置按钮位置 (按钮_停止, 右侧X ＋ 116, 顶部Y ＋ 信息栏高度 ＋ 17, 72, 34)
    设置按钮位置 (按钮_刷新, 右侧X ＋ 196, 顶部Y ＋ 信息栏高度 ＋ 17, 72, 34)
    设置按钮位置 (按钮_打开后台, 右侧X ＋ 276, 顶部Y ＋ 信息栏高度 ＋ 17, 86, 34)
    设置按钮位置 (按钮_同步Cookie, 右侧X ＋ 370, 顶部Y ＋ 信息栏高度 ＋ 17, 96, 34)
    设置按钮位置 (按钮_切换代理, 右侧X ＋ 474, 顶部Y ＋ 信息栏高度 ＋ 17, 88, 34)
    设置按钮位置 (按钮_更多操作, 右侧X ＋ 570, 顶部Y ＋ 信息栏高度 ＋ 17, 88, 34)
.否则
    显示窗口 (工具栏面板, 假)
.如果结束

移动窗口 (浏览器面板, 0, 信息栏高度 ＋ 10 ＋ 工具栏高度 ＋ 10, 右侧宽, 右侧高 － 信息栏高度 － 工具栏高度 － 20)
移动窗口 (浏览器外框, 0, 0, 右侧宽, 右侧高 － 信息栏高度 － 工具栏高度 － 20)
移动窗口 (浏览器画布, 16, 74, 右侧宽 － 32, 右侧高 － 信息栏高度 － 工具栏高度 － 110)

设置标签位置 (标签_浏览器标题, 18, 18, 右侧宽 － 36, 24)
设置标签位置 (标签_浏览器说明, 18, 44, 右侧宽 － 36, 18)
设置标签位置 (标签_浏览器占位, 200, 200, 420, 30)
```

---

## 九、树形框回调

```text
.子程序 树形框选中回调, , , stdcall
.参数 节点ID, 整数型
.参数 上下文, 整数型

当前选中节点ID ＝ 节点ID

.判断开始 (节点ID)
    .如果 (节点ID ＝ 节点_美区环境01)
        当前环境节点ID ＝ 节点ID
        调用 刷新环境页 (“美区-环境01”, “Amazon 店群”, “amazon.com”, “US-Proxy-01”, “运行中”, 92)
    .如果 (节点ID ＝ 节点_美区环境02)
        当前环境节点ID ＝ 节点ID
        调用 刷新环境页 (“美区-环境02”, “Amazon 店群”, “amazon.com”, “US-Proxy-02”, “空闲中”, 88)
    .如果 (节点ID ＝ 节点_TK环境01)
        当前环境节点ID ＝ 节点ID
        调用 刷新环境页 (“TK-环境01”, “TikTok 店群”, “seller-us.tiktok.com”, “US-Proxy-09”, “运行中”, 95)
    .如果 (节点ID ＝ 节点_独立站环境01)
        当前环境节点ID ＝ 节点ID
        调用 刷新环境页 (“独立站-环境01”, “独立站”, “shop.example.com”, “JP-Proxy-03”, “待启动”, 84)

    .如果 (节点ID ＝ 节点_Amazon组)
        调用 刷新模块页 (“Amazon 店群”)
    .如果 (节点ID ＝ 节点_TikTok组)
        调用 刷新模块页 (“TikTok 店群”)
    .如果 (节点ID ＝ 节点_独立站组)
        调用 刷新模块页 (“独立站”)
    .如果 (节点ID ＝ 节点_代理管理)
        调用 刷新模块页 (“代理管理”)
    .如果 (节点ID ＝ 节点_分组管理)
        调用 刷新模块页 (“分组管理”)
    .如果 (节点ID ＝ 节点_RPA自动化)
        调用 刷新模块页 (“RPA自动化”)
    .如果 (节点ID ＝ 节点_插件中心)
        调用 刷新模块页 (“插件中心”)
    .如果 (节点ID ＝ 节点_团队协作)
        调用 刷新模块页 (“团队协作”)
    .如果 (节点ID ＝ 节点_系统设置)
        调用 刷新模块页 (“系统设置”)
    .默认
        调用 刷新模块页 (“环境切换”)
.判断结束

调用 重新布局
```

---

## 十、按钮回调

```text
.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 父窗口, 整数型

.如果 (按钮ID ＝ 按钮_新建环境)
    调用 新建环境
.否则如果 (按钮ID ＝ 按钮_删除环境)
    调用 删除环境
.否则如果 (按钮ID ＝ 按钮_换肤)
    是否深色主题 ＝ 取反 (是否深色主题)
    设置深色模式 (是否深色主题)
    调用 应用主题
    调用 重新布局
.否则如果 (按钮ID ＝ 按钮_启动浏览器)
    设置标签文本_文本 (标签_浏览器说明, “已触发当前环境的草图操作，占位逻辑生效。”)
.如果结束
```

---

## 十一、窗口大小改变回调

```text
.子程序 窗口大小改变回调, , , stdcall
.参数 窗口句柄, 整数型
.参数 新宽度, 整数型
.参数 新高度, 整数型

.如果 (窗口句柄 ≠ 主窗口)
    返回 ()
.如果结束

.如果 (新宽度 ≤ 0 或 新高度 ≤ 0)
    返回 ()
.如果结束

窗口宽度 ＝ 新宽度
窗口高度 ＝ 新高度
调用 重新布局
```

---

## 十二、新建 / 删除环境

```text
.子程序 新建环境

环境计数器 ＝ 环境计数器 ＋ 1
新节点 ＝ 添加子节点 (树形框, 节点_Amazon组, “Amazon-环境” ＋ 到文本 (环境计数器), “●”)
调用 设置环境节点颜色 (新节点, “待启动”)
展开节点 (树形框, 节点_Amazon组)
设置选中节点 (树形框, 新节点)
调用 树形框选中回调 (新节点, 0)
```

```text
.子程序 删除环境

.如果 (当前环境节点ID ＝ 0)
    设置标签文本_文本 (标签_信息副标题, “请先在左侧树形框中选中一个环境节点，再执行删除。”)
    返回 ()
.如果结束

删除节点 (树形框, 当前环境节点ID)
当前环境节点ID ＝ 0
调用 刷新模块页 (“环境切换”)
调用 重新布局
```

---

## 十三、粘贴顺序建议

1. 程序集变量与常量
2. `_启动子程序`
3. `创建界面`
4. `创建标签_简单`
5. `创建按钮_简单`
6. `设置标签文本_文本`
7. `设置按钮文本_文本`
8. `设置窗口标题_文本`
9. `颜色加深`
10. `设置环境节点颜色`
11. `构建树形菜单`
12. `应用主题`
13. `刷新按钮样式`
14. `刷新默认环境`
15. `刷新环境页`
16. `刷新模块页`
17. `重新布局`
18. `树形框选中回调`
19. `按钮点击回调`
20. `窗口大小改变回调`
21. `新建环境`
22. `删除环境`

---

## 十四、最后替换项

你真正需要最后替换的通常只有：

- `创建Emoji窗口_字节集扩展`
- `创建面板`
- `创建标签`
- `创建Emoji按钮_字节集`
- `创建树形框`
- `清空树形框`
- `添加根节点`
- `添加子节点`
- `展开全部节点`
- `展开节点`
- `删除节点`
- `设置选中节点`
- `设置树形框回调`
- `设置按钮点击回调`
- `设置窗口大小改变回调`
- `设置标签文本`
- `设置按钮文本`
- `设置窗口标题`
- `设置按钮圆角`
- `设置按钮悬停颜色`
- `设置节点前景色`

只要这些命令名与你本地模块一致，这份源码稿就已经很接近可直接落地。  
