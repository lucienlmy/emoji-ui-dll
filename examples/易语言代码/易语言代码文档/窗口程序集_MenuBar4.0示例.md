.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 菜单栏句柄, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_更新文案, 整数型
.程序集变量 按钮_切换宽度, 整数型
.程序集变量 按钮_重建菜单, 整数型
.程序集变量 菜单栏窄版, 逻辑型
.程序集变量 文案已更新, 逻辑型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("MenuBar4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 980, 520, 到RGB (64, 158, 255))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("这一页只保留菜单栏相关能力：创建、回调、布局调整、子项文案更新。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 900, 48, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

菜单栏窄版 ＝ 假
文案已更新 ＝ 假
重建菜单栏 ()

emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 ("更新子项文案")
按钮_更新文案 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 160, 152, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 226, 134, 148 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换菜单宽度")
按钮_切换宽度 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 192, 160, 152, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 240, 159, 148, 132 }
文本字节集 ＝ 编码_Ansi到Utf8 ("重建菜单栏")
按钮_重建菜单 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 360, 160, 152, 38, 到ARGB (255, 230, 162, 60))

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 重建菜单栏
.局部变量 文本字节集, 字节集
.局部变量 宽度, 整数型

.如果 (菜单栏句柄 ≠ 0)
    销毁菜单栏 (菜单栏句柄)
.如果结束

菜单栏句柄 ＝ 创建菜单栏 (主窗口句柄)
.如果 (菜单栏窄版)
    宽度 ＝ 360
.否则
    宽度 ＝ 620
.如果结束
设置菜单栏位置 (菜单栏句柄, 24, 84, 宽度, 34)

文本字节集 ＝ 编码_Ansi到Utf8 ("文件")
菜单栏添加项 (菜单栏句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1000)
文本字节集 ＝ 编码_Ansi到Utf8 ("对话框")
菜单栏添加项 (菜单栏句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1100)
文本字节集 ＝ 编码_Ansi到Utf8 ("布局")
菜单栏添加项 (菜单栏句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1200)

文本字节集 ＝ 编码_Ansi到Utf8 ("查看状态")
菜单栏添加子项 (菜单栏句柄, 1000, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1001)
文本字节集 ＝ 编码_Ansi到Utf8 ("显示消息框")
菜单栏添加子项 (菜单栏句柄, 1100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1101)
文本字节集 ＝ 编码_Ansi到Utf8 ("宽 360")
菜单栏添加子项 (菜单栏句柄, 1200, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1201)
文本字节集 ＝ 编码_Ansi到Utf8 ("宽 620")
菜单栏添加子项 (菜单栏句柄, 1200, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 1202)

设置菜单栏回调 (菜单栏句柄, &菜单栏回调)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本字节集, 字节集
.局部变量 宽度, 整数型
.局部变量 成功, 逻辑型

.判断开始 (按钮ID ＝ 按钮_更新文案)
    文案已更新 ＝ 取反 (文案已更新)
    .如果 (文案已更新)
        文本字节集 ＝ 编码_Ansi到Utf8 ("打开消息框")
    .否则
        文本字节集 ＝ 编码_Ansi到Utf8 ("显示消息框")
    .如果结束
    成功 ＝ 更新菜单栏子项文字 (菜单栏句柄, 1100, 1101, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    .如果 (成功)
        设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    .否则
        文本字节集 ＝ 编码_Ansi到Utf8 ("更新菜单栏子项文案失败。")
        设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    .如果结束

.判断 (按钮ID ＝ 按钮_切换宽度)
    菜单栏窄版 ＝ 取反 (菜单栏窄版)
    .如果 (菜单栏窄版)
        宽度 ＝ 360
    .否则
        宽度 ＝ 620
    .如果结束
    设置菜单栏位置 (菜单栏句柄, 24, 84, 宽度, 34)
    文本字节集 ＝ 编码_Ansi到Utf8 ("菜单栏宽度已改为：") ＋ 编码_Ansi到Utf8 (到文本 (宽度))
    设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.判断 (按钮ID ＝ 按钮_重建菜单)
    重建菜单栏 ()
    文本字节集 ＝ 编码_Ansi到Utf8 ("菜单栏已重建。")
    设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.默认

.判断结束

.子程序 菜单栏回调, , , stdcall
.参数 menu_id, 整数型
.参数 item_id, 整数型
.局部变量 标题字节集, 字节集
.局部变量 内容字节集, 字节集
.局部变量 图标字节集, 字节集

.判断开始 (item_id ＝ 1001)
    内容字节集 ＝ 编码_Ansi到Utf8 ("菜单栏已收到“查看状态”点击。")
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.判断 (item_id ＝ 1101)
    标题字节集 ＝ 编码_Ansi到Utf8 ("MenuBar 消息框")
    内容字节集 ＝ 编码_Ansi到Utf8 ("这是从 MenuBar 子菜单打开的 MessageBox。")
    图标字节集 ＝ { 240, 159, 147, 129 }
    信息提示框_ (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.判断 (item_id ＝ 1201)
    菜单栏窄版 ＝ 真
    设置菜单栏位置 (菜单栏句柄, 24, 84, 360, 34)
    内容字节集 ＝ 编码_Ansi到Utf8 ("菜单栏已切换到 360 宽度。")
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.判断 (item_id ＝ 1202)
    菜单栏窄版 ＝ 假
    设置菜单栏位置 (菜单栏句柄, 24, 84, 620, 34)
    内容字节集 ＝ 编码_Ansi到Utf8 ("菜单栏已切换到 620 宽度。")
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.默认
    内容字节集 ＝ 编码_Ansi到Utf8 ("收到菜单栏回调，item_id=") ＋ 编码_Ansi到Utf8 (到文本 (item_id))
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.判断结束

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
