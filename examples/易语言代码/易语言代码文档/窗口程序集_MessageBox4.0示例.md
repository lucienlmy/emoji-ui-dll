.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_普通消息, 整数型
.程序集变量 按钮_成功消息, 整数型
.程序集变量 按钮_警告消息, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("MessageBox4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 760, 420, 到RGB (64, 158, 255))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("这一页只保留 MessageBox 触发入口，用于验证 UTF-8 标题、内容和图标。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 680, 50, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

emoji字节集 ＝ { 240, 159, 146, 172 }
文本字节集 ＝ 编码_Ansi到Utf8 ("普通消息")
按钮_普通消息 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 120, 140, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 226, 156, 133 }
文本字节集 ＝ 编码_Ansi到Utf8 ("成功消息")
按钮_成功消息 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 184, 120, 140, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 226, 154, 160, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 ("警告消息")
按钮_警告消息 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 344, 120, 140, 38, 到ARGB (255, 230, 162, 60))

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 标题字节集, 字节集
.局部变量 内容字节集, 字节集
.局部变量 图标字节集, 字节集
.局部变量 状态字节集, 字节集

.判断开始 (按钮ID ＝ 按钮_普通消息)
    标题字节集 ＝ 编码_Ansi到Utf8 ("普通消息")
    内容字节集 ＝ 编码_Ansi到Utf8 ("这是 MessageBox 4.0 demo 中的普通消息。")
    图标字节集 ＝ { 240, 159, 146, 172 }
    信息提示框_ (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
    状态字节集 ＝ 编码_Ansi到Utf8 ("已打开普通 MessageBox。")
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 按钮_成功消息)
    标题字节集 ＝ 编码_Ansi到Utf8 ("成功消息")
    内容字节集 ＝ 编码_Ansi到Utf8 ("操作已完成。这个弹窗用于单独验证 success 风格文案。")
    图标字节集 ＝ { 226, 156, 133 }
    信息提示框_ (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
    状态字节集 ＝ 编码_Ansi到Utf8 ("已打开成功 MessageBox。")
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 按钮_警告消息)
    标题字节集 ＝ 编码_Ansi到Utf8 ("警告消息")
    内容字节集 ＝ 编码_Ansi到Utf8 ("请继续检查消息框标题、正文和按钮布局。")
    图标字节集 ＝ { 226, 154, 160, 239, 184, 143 }
    信息提示框_ (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
    状态字节集 ＝ 编码_Ansi到Utf8 ("已打开警告 MessageBox。")
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.默认

.判断结束

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
