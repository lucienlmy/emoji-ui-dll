.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 当前通知句柄, 整数型
.程序集变量 按钮_信息通知, 整数型
.程序集变量 按钮_成功通知, 整数型
.程序集变量 按钮_警告通知, 整数型
.程序集变量 按钮_错误通知, 整数型
.程序集变量 按钮_关闭通知, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("Notification4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 860, 480, 到RGB (64, 158, 255))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("Notification 支持 info / success / warning / error 四种类型，以及点击 / 关闭回调。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 780, 60, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

emoji字节集 ＝ { 240, 159, 146, 172 }
文本字节集 ＝ 编码_Ansi到Utf8 ("信息通知")
按钮_信息通知 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 116, 132, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 226, 156, 133 }
文本字节集 ＝ 编码_Ansi到Utf8 ("成功通知")
按钮_成功通知 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 172, 116, 132, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 226, 154, 160, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 ("警告通知")
按钮_警告通知 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 320, 116, 132, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 226, 157, 140 }
文本字节集 ＝ 编码_Ansi到Utf8 ("错误通知")
按钮_错误通知 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 468, 116, 132, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 240, 159, 151, 145 }
文本字节集 ＝ 编码_Ansi到Utf8 ("关闭当前通知")
按钮_关闭通知 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 616, 116, 148, 38, 到ARGB (255, 144, 147, 153))

当前通知句柄 ＝ 0
设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 标题字节集, 字节集
.局部变量 内容字节集, 字节集
.局部变量 状态字节集, 字节集

.判断开始 (按钮ID ＝ 按钮_信息通知)
    标题字节集 ＝ 编码_Ansi到Utf8 ("信息通知")
    内容字节集 ＝ 编码_Ansi到Utf8 ("这是 info Notification。3 秒后自动关闭。")
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 0, 0, 3000)
    设置通知回调 (当前通知句柄, &通知回调)
    状态字节集 ＝ 编码_Ansi到Utf8 ("已弹出信息通知。")
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 按钮_成功通知)
    标题字节集 ＝ 编码_Ansi到Utf8 ("成功通知")
    内容字节集 ＝ 编码_Ansi到Utf8 ("这是 success Notification。")
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 1, 0, 3000)
    设置通知回调 (当前通知句柄, &通知回调)
    状态字节集 ＝ 编码_Ansi到Utf8 ("已弹出成功通知。")
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 按钮_警告通知)
    标题字节集 ＝ 编码_Ansi到Utf8 ("警告通知")
    内容字节集 ＝ 编码_Ansi到Utf8 ("这是 warning Notification。")
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 2, 2, 3000)
    设置通知回调 (当前通知句柄, &通知回调)
    状态字节集 ＝ 编码_Ansi到Utf8 ("已弹出警告通知。")
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 按钮_错误通知)
    标题字节集 ＝ 编码_Ansi到Utf8 ("错误通知")
    内容字节集 ＝ 编码_Ansi到Utf8 ("这是 error Notification。")
    当前通知句柄 ＝ 显示通知 (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 3, 2, 0)
    设置通知回调 (当前通知句柄, &通知回调)
    状态字节集 ＝ 编码_Ansi到Utf8 ("已弹出错误通知，需手动关闭。")
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.判断 (按钮ID ＝ 按钮_关闭通知)
    .如果 (当前通知句柄 ≠ 0)
        关闭通知 (当前通知句柄)
        当前通知句柄 ＝ 0
        状态字节集 ＝ 编码_Ansi到Utf8 ("已主动关闭当前通知。")
    .否则
        状态字节集 ＝ 编码_Ansi到Utf8 ("当前没有可关闭的通知。")
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.默认

.判断结束

.子程序 通知回调, , , stdcall
.参数 hNotification, 整数型
.参数 event_type, 整数型
.局部变量 状态字节集, 字节集

.如果 (event_type ＝ 0)
    当前通知句柄 ＝ 0
    状态字节集 ＝ 编码_Ansi到Utf8 ("Notification 回调：用户关闭了通知。")
.否则
    状态字节集 ＝ 编码_Ansi到Utf8 ("Notification 回调：用户点击了通知内容。")
.如果结束
设置标签文本 (标签_状态, 取变量数据地址 (状态字节集), 取字节集长度 (状态字节集))

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
