.版本 2
.支持库 spec
.支持库 eAPI

.程序集 程序集1

' ========== 程序集变量 ==========
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_结果, 整数型
.程序集变量 TabControl句柄, 整数型
.程序集变量 TabControl是否可见, 整数型
.程序集变量 TabControl是否启用, 整数型

' ----- 按钮ID变量 -----
.程序集变量 按钮_设置外观, 整数型
.程序集变量 按钮_禁用标签页, 整数型
.程序集变量 按钮_隐藏标签页, 整数型
.程序集变量 按钮_设置内容背景色, 整数型
.程序集变量 按钮_启用关闭按钮, 整数型
.程序集变量 按钮_启用拖拽, 整数型
.程序集变量 按钮_设置标签栏位置, 整数型
.程序集变量 按钮_设置对齐方式, 整数型
.程序集变量 按钮_启用滚动, 整数型
.程序集变量 按钮_清空标签页, 整数型
.程序集变量 按钮_插入标签页, 整数型
.程序集变量 按钮_移动标签页, 整数型
.程序集变量 按钮_查找标签页, 整数型
.程序集变量 按钮_查询状态, 整数型
.程序集变量 按钮_获取设置标题, 整数型
.程序集变量 按钮_获取设置位置, 整数型
.程序集变量 按钮_切换可视, 整数型
.程序集变量 按钮_切换启用, 整数型

' ----- 标签栏位置循环变量 -----
.程序集变量 当前标签栏位置, 整数型
.程序集变量 当前对齐方式, 整数型
.程序集变量 Tab2已隐藏, 整数型
.程序集变量 拖拽已启用, 整数型
.程序集变量 关闭按钮已启用, 整数型
.程序集变量 滚动模式, 整数型

' ========== 启动子程序 ==========
.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 utf8字节集, 字节集
.局部变量 按钮文本字节集, 字节集
.局部变量 字体名utf8, 字节集
.局部变量 Tab标题字节集, 字节集
.局部变量 内容窗口句柄, 整数型
.局部变量 子标签句柄, 整数型
.局部变量 子按钮ID, 整数型

' ===== 创建窗口 =====
' "📑 TabControl完整示例"  📑 = { 240, 159, 147, 145 }
utf8字节集 ＝ { 240, 159, 147, 145, 32 } ＋ 编码_Ansi到Utf8 ("TabControl完整示例（v3.0）")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), -1, -1, 1000, 780, 到RGB (50, 50, 60), 到ARGB (255, 245, 247, 250))

' ===== 创建结果显示标签 =====
utf8字节集 ＝ 编码_Ansi到Utf8 ("点击下方按钮演示TabControl各项功能")
字体名utf8 ＝ 编码_Ansi到Utf8 ("微软雅黑")
标签_结果 ＝ 创建标签 (主窗口句柄, 20, 10, 960, 80, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

' ===== 创建TabControl =====
TabControl句柄 ＝ 创建TabControl (主窗口句柄, 20, 100, 960, 380)

' ===== 添加标签页（含Emoji标题）=====
' Tab0: "🏠 首页"  🏠 = { 240, 159, 143, 160 }
Tab标题字节集 ＝ { 240, 159, 143, 160, 32 } ＋ 编码_Ansi到Utf8 ("首页")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' Tab1: "⚙ 设置"  ⚙ = { 226, 154, 153 }
Tab标题字节集 ＝ { 226, 154, 153, 32 } ＋ 编码_Ansi到Utf8 ("设置")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' Tab2: "📊 数据"  📊 = { 240, 159, 147, 138 }
Tab标题字节集 ＝ { 240, 159, 147, 138, 32 } ＋ 编码_Ansi到Utf8 ("数据")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' Tab3: "🔧 工具"  🔧 = { 240, 159, 148, 167 }
Tab标题字节集 ＝ { 240, 159, 148, 167, 32 } ＋ 编码_Ansi到Utf8 ("工具")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' Tab4: "❓ 帮助"  ❓ = { 226, 157, 147 }
Tab标题字节集 ＝ { 226, 157, 147, 32 } ＋ 编码_Ansi到Utf8 ("帮助")
添加Tab页 (TabControl句柄, 取变量数据地址 (Tab标题字节集), 取字节集长度 (Tab标题字节集), 0)

' ===== 在标签页内容窗口中创建子控件 =====
' Tab0 首页：添加欢迎标签和按钮
内容窗口句柄 ＝ 获取Tab内容窗口 (TabControl句柄, 0)
utf8字节集 ＝ { 240, 159, 145, 139, 32 } ＋ 编码_Ansi到Utf8 ("欢迎使用TabControl完整示例！本示例演示了v3.0所有新增功能。")
字体名utf8 ＝ 编码_Ansi到Utf8 ("微软雅黑")
子标签句柄 ＝ 创建标签 (内容窗口句柄, 20, 20, 900, 60, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 236, 245, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 14, 真, 假, 假, 0, 真)

' Tab1 设置：添加说明标签
内容窗口句柄 ＝ 获取Tab内容窗口 (TabControl句柄, 1)
utf8字节集 ＝ 编码_Ansi到Utf8 ("这是设置页面的内容区域，可以放置各种配置控件。")
子标签句柄 ＝ 创建标签 (内容窗口句柄, 20, 20, 900, 40, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 13, 假, 假, 假, 0, 假)

' Tab2 数据：添加说明标签
内容窗口句柄 ＝ 获取Tab内容窗口 (TabControl句柄, 2)
' "📊 这是第3页（数据页）"
utf8字节集 ＝ { 240, 159, 147, 138, 32 } ＋ 编码_Ansi到Utf8 ("这是第3页（数据页），可以放置表格、图表等数据展示控件。")
子标签句柄 ＝ 创建标签 (内容窗口句柄, 20, 20, 900, 40, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 230, 162, 60), 到ARGB (255, 255, 249, 235), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 13, 假, 假, 假, 0, 假)

' Tab3 工具：添加说明标签
内容窗口句柄 ＝ 获取Tab内容窗口 (TabControl句柄, 3)
' "🔧 这是第4页（工具页）"
utf8字节集 ＝ { 240, 159, 148, 167, 32 } ＋ 编码_Ansi到Utf8 ("这是第4页（工具页），可以放置各种实用工具控件。")
子标签句柄 ＝ 创建标签 (内容窗口句柄, 20, 20, 900, 40, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 103, 194, 58), 到ARGB (255, 240, 249, 235), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 13, 假, 假, 假, 0, 假)

' Tab4 帮助：添加说明标签
内容窗口句柄 ＝ 获取Tab内容窗口 (TabControl句柄, 4)
' "❓ 这是第5页（帮助页）"
utf8字节集 ＝ { 226, 157, 147, 32 } ＋ 编码_Ansi到Utf8 ("这是第5页（帮助页），可以放置帮助文档和常见问题。")
子标签句柄 ＝ 创建标签 (内容窗口句柄, 20, 20, 900, 40, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 144, 147, 153), 到ARGB (255, 245, 245, 248), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 13, 假, 假, 假, 0, 假)

' ===== 设置外观：尺寸、字体、颜色、指示条颜色、内边距 =====
设置标签页尺寸 (TabControl句柄, 140, 38)

字体名utf8 ＝ 编码_Ansi到Utf8 ("Segoe UI Emoji")
设置Tab标签字体 (TabControl句柄, 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 13.0)

设置Tab标签颜色 (TabControl句柄, 到ARGB (255, 255, 255, 255), 到ARGB (255, 245, 247, 250), 到ARGB (255, 64, 158, 255), 到ARGB (255, 96, 98, 102))

设置指示条颜色 (TabControl句柄, 到ARGB (255, 64, 158, 255))

设置标签内边距 (TabControl句柄, 8, 2)

' ===== 设置标签页图标（注释说明）=====
' 如需设置PNG图标，可使用以下方式：
' .局部变量 图标数据, 字节集
' 图标数据 ＝ 读入文件 ("icon.png")
' 设置标签页图标 (TabControl句柄, 0, 取变量数据地址 (图标数据), 取字节集长度 (图标数据))
' 清除图标：设置标签页图标 (TabControl句柄, 0, 0, 0)

' ===== 设置内容区域背景色 =====
设置标签页内容背景色 (TabControl句柄, 0, 到ARGB (255, 236, 245, 255))
设置所有标签页内容背景色 (TabControl句柄, 到ARGB (255, 250, 250, 252))

' ===== 关闭按钮（默认关闭，通过按钮启用）=====
设置标签页关闭回调 (TabControl句柄, &标签页关闭回调)

' ===== 设置右键回调 =====
设置标签页右键回调 (TabControl句柄, &标签页右键回调)

' ===== 拖拽排序（默认关闭，通过按钮启用）=====

' ===== 设置双击回调 =====
设置标签页双击回调 (TabControl句柄, &标签页双击回调)

' ===== 设置Tab切换回调 =====
设置Tab切换回调 (TabControl句柄, &Tab切换回调)

' ===== 设置窗口大小改变回调 =====
设置窗口大小改变回调 (&窗口大小改变回调)

' ===== 初始化状态变量 =====
TabControl是否可见 ＝ 1
TabControl是否启用 ＝ 1
当前标签栏位置 ＝ 0
当前对齐方式 ＝ 0
Tab2已隐藏 ＝ 0
拖拽已启用 ＝ 0
关闭按钮已启用 ＝ 0
滚动模式 ＝ 1

' ===== 创建操作按钮（第一行：外观与单页控制）=====

' "🎨 设置外观"  🎨 = { 240, 159, 142, 168 }
utf8字节集 ＝ { 240, 159, 142, 168 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("设置外观")
按钮_设置外观 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 495, 145, 38, 到ARGB (255, 64, 158, 255))

' "🚫 禁用标签页"  🚫 = { 240, 159, 154, 171 }
utf8字节集 ＝ { 240, 159, 154, 171 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("禁用标签页")
按钮_禁用标签页 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 175, 495, 145, 38, 到ARGB (255, 230, 162, 60))

' "👁 隐藏标签页"  👁 = { 240, 159, 145, 129 }
utf8字节集 ＝ { 240, 159, 145, 129 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("隐藏标签页")
按钮_隐藏标签页 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 330, 495, 145, 38, 到ARGB (255, 230, 162, 60))

' "🎨 内容背景色"  🎨 = { 240, 159, 142, 168 }
utf8字节集 ＝ { 240, 159, 142, 168 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("内容背景色")
按钮_设置内容背景色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 485, 495, 145, 38, 到ARGB (255, 64, 158, 255))

' "❌ 启用关闭按钮"  ❌ = { 226, 157, 140 }
utf8字节集 ＝ { 226, 157, 140 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("启用关闭按钮")
按钮_启用关闭按钮 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 640, 495, 155, 38, 到ARGB (255, 245, 108, 108))

' "🔀 启用拖拽"  🔀 = { 240, 159, 148, 128 }
utf8字节集 ＝ { 240, 159, 148, 128 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("启用拖拽")
按钮_启用拖拽 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 805, 495, 145, 38, 到ARGB (255, 103, 194, 58))

' ===== 创建操作按钮（第二行：布局与批量操作）=====

' "📍 标签栏位置"  📍 = { 240, 159, 147, 141 }
utf8字节集 ＝ { 240, 159, 147, 141 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("标签栏位置")
按钮_设置标签栏位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 543, 145, 38, 到ARGB (255, 144, 147, 153))

' "📐 对齐方式"  📐 = { 240, 159, 147, 144 }
utf8字节集 ＝ { 240, 159, 147, 144 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("对齐方式")
按钮_设置对齐方式 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 175, 543, 145, 38, 到ARGB (255, 144, 147, 153))

' "📜 启用滚动"  📜 = { 240, 159, 147, 156 }
utf8字节集 ＝ { 240, 159, 147, 156 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("启用滚动")
按钮_启用滚动 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 330, 543, 145, 38, 到ARGB (255, 144, 147, 153))

' "🗑 清空标签页"  🗑 = { 240, 159, 151, 145 }
utf8字节集 ＝ { 240, 159, 151, 145 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("清空标签页")
按钮_清空标签页 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 485, 543, 145, 38, 到ARGB (255, 245, 108, 108))

' "➕ 插入标签页"  ➕ = { 226, 158, 149 }
utf8字节集 ＝ { 226, 158, 149 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("插入标签页")
按钮_插入标签页 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 640, 543, 155, 38, 到ARGB (255, 103, 194, 58))

' "🔄 移动标签页"  🔄 = { 240, 159, 148, 132 }
utf8字节集 ＝ { 240, 159, 148, 132 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("移动标签页")
按钮_移动标签页 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 805, 543, 145, 38, 到ARGB (255, 103, 194, 58))

' ===== 创建操作按钮（第三行：查询与控制）=====

' "🔍 查找标签页"  🔍 = { 240, 159, 148, 141 }
utf8字节集 ＝ { 240, 159, 148, 141 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("查找标签页")
按钮_查找标签页 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 591, 145, 38, 到ARGB (255, 64, 158, 255))

' "📋 查询状态"  📋 = { 240, 159, 147, 139 }
utf8字节集 ＝ { 240, 159, 147, 139 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("查询状态")
按钮_查询状态 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 175, 591, 145, 38, 到ARGB (255, 64, 158, 255))

' "📝 获取/设置标题"  📝 = { 240, 159, 147, 157 }
utf8字节集 ＝ { 240, 159, 147, 157 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取/设置标题")
按钮_获取设置标题 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 330, 591, 155, 38, 到ARGB (255, 64, 158, 255))

' "📐 获取/设置位置"  📐 = { 240, 159, 147, 144 }
utf8字节集 ＝ { 240, 159, 147, 144 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("获取/设置位置")
按钮_获取设置位置 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 495, 591, 155, 38, 到ARGB (255, 64, 158, 255))

' "👁 切换显示/隐藏"  👁 = { 240, 159, 145, 129 }
utf8字节集 ＝ { 240, 159, 145, 129 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("切换显示/隐藏")
按钮_切换可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 660, 591, 155, 38, 到ARGB (255, 180, 100, 100))

' "⚡ 切换启用/禁用"  ⚡ = { 226, 154, 161 }
utf8字节集 ＝ { 226, 154, 161 }
按钮文本字节集 ＝ 编码_Ansi到Utf8 ("切换启用/禁用")
按钮_切换启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 825, 591, 155, 38, 到ARGB (255, 180, 100, 100))

' ===== 设置按钮点击回调 =====
设置按钮点击回调 (&按钮点击回调)

' ===== 设置消息循环主窗口并运行 =====
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)


' ========== Tab切换回调 ==========
.子程序 Tab切换回调, , , stdcall
.参数 hTabControl, 整数型
.参数 选中索引, 整数型
.局部变量 结果字节集, 字节集
.局部变量 标题长度, 整数型
.局部变量 标题缓冲区, 字节集

标题长度 ＝ 获取Tab页标题 (hTabControl, 选中索引, 0, 0)
.如果 (标题长度 ＞ 0)
    标题缓冲区 ＝ 取空白字节集 (标题长度)
    获取Tab页标题 (hTabControl, 选中索引, 取变量数据地址 (标题缓冲区), 标题长度)
    结果字节集 ＝ 编码_Ansi到Utf8 ("【Tab切换】已切换到索引：") ＋ 编码_Ansi到Utf8 (到文本 (选中索引)) ＋ 编码_Ansi到Utf8 ("  标题：") ＋ 标题缓冲区
.否则
    结果字节集 ＝ 编码_Ansi到Utf8 ("【Tab切换】已切换到索引：") ＋ 编码_Ansi到Utf8 (到文本 (选中索引))
.如果结束
设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

' ========== 标签页关闭回调 ==========
.子程序 标签页关闭回调, , , stdcall
.参数 hTabControl, 整数型
.参数 索引, 整数型
.局部变量 结果字节集, 字节集

' 执行删除操作
移除Tab页 (hTabControl, 索引)
结果字节集 ＝ 编码_Ansi到Utf8 ("【关闭回调】已关闭并移除标签页索引：") ＋ 编码_Ansi到Utf8 (到文本 (索引)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("当前Tab数量：") ＋ 编码_Ansi到Utf8 (到文本 (获取Tab数量 (hTabControl)))
设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

' ========== 标签页右键回调 ==========
.子程序 标签页右键回调, , , stdcall
.参数 hTabControl, 整数型
.参数 索引, 整数型
.参数 X, 整数型
.参数 Y, 整数型
.局部变量 结果字节集, 字节集

结果字节集 ＝ 编码_Ansi到Utf8 ("【右键回调】标签页索引：") ＋ 编码_Ansi到Utf8 (到文本 (索引)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("鼠标屏幕坐标：X=") ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ 编码_Ansi到Utf8 (", Y=") ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("（可在此弹出右键菜单）")
设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

' ========== 标签页双击回调 ==========
.子程序 标签页双击回调, , , stdcall
.参数 hTabControl, 整数型
.参数 索引, 整数型
.局部变量 结果字节集, 字节集

结果字节集 ＝ 编码_Ansi到Utf8 ("【双击回调】双击了标签页索引：") ＋ 编码_Ansi到Utf8 (到文本 (索引)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("（可在此实现双击重命名等功能）")
设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))


' ========== 按钮点击回调 ==========
.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 结果字节集, 字节集
.局部变量 字体名utf8, 字节集
.局部变量 标题长度, 整数型
.局部变量 标题缓冲区, 字节集
.局部变量 新标题字节集, 字节集
.局部变量 X, 整数型
.局部变量 Y, 整数型
.局部变量 宽度, 整数型
.局部变量 高度, 整数型
.局部变量 可视状态, 整数型
.局部变量 启用状态, 整数型
.局部变量 是否选中, 整数型
.局部变量 Tab数量, 整数型
.局部变量 查找索引, 整数型
.局部变量 查找标题字节集, 字节集
.局部变量 新索引, 整数型
.局部变量 i, 整数型
.局部变量 位置名称, 字节集
.局部变量 对齐名称, 字节集

.判断开始 (按钮ID ＝ 按钮_设置外观)
    ' 演示设置外观：尺寸、字体、颜色、指示条、内边距
    设置标签页尺寸 (TabControl句柄, 160, 42)
    字体名utf8 ＝ 编码_Ansi到Utf8 ("微软雅黑")
    设置Tab标签字体 (TabControl句柄, 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 14.0)
    设置Tab标签颜色 (TabControl句柄, 到ARGB (255, 64, 158, 255), 到ARGB (255, 235, 238, 245), 到ARGB (255, 255, 255, 255), 到ARGB (255, 96, 98, 102))
    设置指示条颜色 (TabControl句柄, 到ARGB (255, 103, 194, 58))
    设置标签内边距 (TabControl句柄, 12, 4)
    结果字节集 ＝ 编码_Ansi到Utf8 ("【设置外观】已更新：") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  尺寸：160×42") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  字体：微软雅黑 14号") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  选中背景：蓝色，未选中背景：浅灰") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  指示条：绿色，内边距：12×4")
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_禁用标签页)
    ' 演示启用/禁用单个标签页 + 获取启用状态
    启用状态 ＝ 获取标签页启用状态 (TabControl句柄, 1)
    .如果 (启用状态 ＝ 1)
        启用禁用标签页 (TabControl句柄, 1, 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【禁用标签页】已禁用Tab[1]（设置页）") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击可重新启用")
    .否则
        启用禁用标签页 (TabControl句柄, 1, 1)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【启用标签页】已重新启用Tab[1]（设置页）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_隐藏标签页)
    ' 演示显示/隐藏单个标签页（切换模式）
    Tab数量 ＝ 获取Tab数量 (TabControl句柄)
    .如果真 (Tab数量 ＞ 2)
        .如果 (Tab2已隐藏 ＝ 0)
            显示隐藏标签页 (TabControl句柄, 2, 0)
            Tab2已隐藏 ＝ 1
            结果字节集 ＝ 编码_Ansi到Utf8 ("【隐藏标签页】已隐藏Tab[2]") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击可恢复显示")
        .否则
            显示隐藏标签页 (TabControl句柄, 2, 1)
            Tab2已隐藏 ＝ 0
            结果字节集 ＝ 编码_Ansi到Utf8 ("【显示标签页】已恢复显示Tab[2]")
        .如果结束
    .如果真结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_设置内容背景色)
    ' 演示设置每个标签页不同的内容区域背景色（用明显的颜色）
    Tab数量 ＝ 获取Tab数量 (TabControl句柄)
    .如果真 (Tab数量 ＞ 0)
        设置标签页内容背景色 (TabControl句柄, 0, 到ARGB (255, 200, 230, 255))
    .如果真结束
    .如果真 (Tab数量 ＞ 1)
        设置标签页内容背景色 (TabControl句柄, 1, 到ARGB (255, 255, 230, 200))
    .如果真结束
    .如果真 (Tab数量 ＞ 2)
        设置标签页内容背景色 (TabControl句柄, 2, 到ARGB (255, 200, 255, 200))
    .如果真结束
    .如果真 (Tab数量 ＞ 3)
        设置标签页内容背景色 (TabControl句柄, 3, 到ARGB (255, 255, 200, 255))
    .如果真结束
    .如果真 (Tab数量 ＞ 4)
        设置标签页内容背景色 (TabControl句柄, 4, 到ARGB (255, 255, 255, 200))
    .如果真结束
    结果字节集 ＝ 编码_Ansi到Utf8 ("【内容背景色】已为每个标签页设置不同背景色：") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[0]首页：浅蓝色") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[1]设置：浅橙色") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[2]数据：浅绿色") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[3]工具：浅紫色") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[4]帮助：浅黄色") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("请切换标签页查看效果")
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_启用关闭按钮)
    ' 切换关闭按钮开关
    .如果 (关闭按钮已启用 ＝ 0)
        设置标签页可关闭 (TabControl句柄, 1)
        关闭按钮已启用 ＝ 1
        结果字节集 ＝ 编码_Ansi到Utf8 ("【关闭按钮】已启用") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("点击标签页上的 × 按钮可关闭标签页") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击可禁用")
    .否则
        设置标签页可关闭 (TabControl句柄, 0)
        关闭按钮已启用 ＝ 0
        结果字节集 ＝ 编码_Ansi到Utf8 ("【关闭按钮】已禁用") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击可启用")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_启用拖拽)
    ' 切换拖拽排序开关
    .如果 (拖拽已启用 ＝ 0)
        设置标签页可拖拽 (TabControl句柄, 1)
        拖拽已启用 ＝ 1
        结果字节集 ＝ 编码_Ansi到Utf8 ("【拖拽排序】已启用") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("按住鼠标左键拖动标签页可重新排序") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击可禁用")
    .否则
        设置标签页可拖拽 (TabControl句柄, 0)
        拖拽已启用 ＝ 0
        结果字节集 ＝ 编码_Ansi到Utf8 ("【拖拽排序】已禁用") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击可启用")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_设置标签栏位置)
    ' 循环切换标签栏位置：上→下→左→右→上
    当前标签栏位置 ＝ 当前标签栏位置 ＋ 1
    .如果真 (当前标签栏位置 ＞ 3)
        当前标签栏位置 ＝ 0
    .如果真结束
    设置标签栏位置 (TabControl句柄, 当前标签栏位置)
    .判断开始 (当前标签栏位置 ＝ 0)
        位置名称 ＝ 编码_Ansi到Utf8 ("上")
    .判断 (当前标签栏位置 ＝ 1)
        位置名称 ＝ 编码_Ansi到Utf8 ("下")
    .判断 (当前标签栏位置 ＝ 2)
        位置名称 ＝ 编码_Ansi到Utf8 ("左")
    .判断 (当前标签栏位置 ＝ 3)
        位置名称 ＝ 编码_Ansi到Utf8 ("右")
    .默认
        位置名称 ＝ 编码_Ansi到Utf8 ("未知")
    .判断结束
    结果字节集 ＝ 编码_Ansi到Utf8 ("【标签栏位置】已设置为：") ＋ 位置名称 ＋ 编码_Ansi到Utf8 ("（") ＋ 编码_Ansi到Utf8 (到文本 (当前标签栏位置)) ＋ 编码_Ansi到Utf8 ("）") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击切换到下一个位置")
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_设置对齐方式)
    ' 循环切换对齐方式：左→居中→右→左
    当前对齐方式 ＝ 当前对齐方式 ＋ 1
    .如果真 (当前对齐方式 ＞ 2)
        当前对齐方式 ＝ 0
    .如果真结束
    设置标签对齐方式 (TabControl句柄, 当前对齐方式)
    .判断开始 (当前对齐方式 ＝ 0)
        对齐名称 ＝ 编码_Ansi到Utf8 ("左对齐")
    .判断 (当前对齐方式 ＝ 1)
        对齐名称 ＝ 编码_Ansi到Utf8 ("居中")
    .判断 (当前对齐方式 ＝ 2)
        对齐名称 ＝ 编码_Ansi到Utf8 ("右对齐")
    .默认
        对齐名称 ＝ 编码_Ansi到Utf8 ("未知")
    .判断结束
    结果字节集 ＝ 编码_Ansi到Utf8 ("【对齐方式】已设置为：") ＋ 对齐名称 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击切换到下一个对齐方式")
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_启用滚动)
    ' 切换滚动/多行模式
    .如果 (滚动模式 ＝ 1)
        ' 当前是滚动模式，切换到多行模式
        设置标签栏可滚动 (TabControl句柄, 0)
        滚动模式 ＝ 0
        结果字节集 ＝ 编码_Ansi到Utf8 ("【多行模式】已切换到多行标签模式") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("标签页过多时自动换行显示") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击切换回滚动模式")
    .否则
        ' 当前是多行模式，切换到滚动模式
        设置标签栏可滚动 (TabControl句柄, 1)
        滚动模式 ＝ 1
        结果字节集 ＝ 编码_Ansi到Utf8 ("【滚动模式】已切换到单行滚动模式") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("标签页过多时显示滚动箭头") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击切换回多行模式")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_清空标签页)
    ' 演示清空所有标签页后重新添加
    清空所有标签页 (TabControl句柄)
    结果字节集 ＝ 编码_Ansi到Utf8 ("【清空标签页】已清空所有标签页") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("正在重新添加默认标签页...")
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    ' 重新添加默认标签页
    ' "🏠 首页"
    新标题字节集 ＝ { 240, 159, 143, 160, 32 } ＋ 编码_Ansi到Utf8 ("首页")
    添加Tab页 (TabControl句柄, 取变量数据地址 (新标题字节集), 取字节集长度 (新标题字节集), 0)
    ' "⚙ 设置"
    新标题字节集 ＝ { 226, 154, 153, 32 } ＋ 编码_Ansi到Utf8 ("设置")
    添加Tab页 (TabControl句柄, 取变量数据地址 (新标题字节集), 取字节集长度 (新标题字节集), 0)
    ' "📊 数据"
    新标题字节集 ＝ { 240, 159, 147, 138, 32 } ＋ 编码_Ansi到Utf8 ("数据")
    添加Tab页 (TabControl句柄, 取变量数据地址 (新标题字节集), 取字节集长度 (新标题字节集), 0)
    结果字节集 ＝ 编码_Ansi到Utf8 ("【清空标签页】已清空并重新添加3个标签页") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("当前Tab数量：") ＋ 编码_Ansi到Utf8 (到文本 (获取Tab数量 (TabControl句柄)))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_插入标签页)
    ' 演示在指定位置插入标签页
    ' "➕ 新页面"  ➕ = { 226, 158, 149 }
    新标题字节集 ＝ { 226, 158, 149, 32 } ＋ 编码_Ansi到Utf8 ("新页面")
    新索引 ＝ 插入标签页 (TabControl句柄, 0, 取变量数据地址 (新标题字节集), 取字节集长度 (新标题字节集), 0)
    .如果 (新索引 ≥ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【插入标签页】已在位置0插入新标签页") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("实际索引：") ＋ 编码_Ansi到Utf8 (到文本 (新索引)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("当前Tab数量：") ＋ 编码_Ansi到Utf8 (到文本 (获取Tab数量 (TabControl句柄)))
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【插入标签页】插入失败（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_移动标签页)
    ' 演示移动标签页位置：将第一个移到最后
    Tab数量 ＝ 获取Tab数量 (TabControl句柄)
    .如果 (Tab数量 ＞ 1)
        移动标签页 (TabControl句柄, 0, Tab数量 ＋ -1)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【移动标签页】已将Tab[0]移动到Tab[") ＋ 编码_Ansi到Utf8 (到文本 (Tab数量 ＋ -1)) ＋ 编码_Ansi到Utf8 ("]")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【移动标签页】标签页数量不足，无法移动")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_查找标签页)
    ' 演示根据标题查找标签页
    查找标题字节集 ＝ { 226, 154, 153, 32 } ＋ 编码_Ansi到Utf8 ("设置")
    查找索引 ＝ 根据标题查找标签页 (TabControl句柄, 取变量数据地址 (查找标题字节集), 取字节集长度 (查找标题字节集))
    .如果 (查找索引 ≥ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【查找标签页】查找标题") ＋ 查找标题字节集 ＋ 编码_Ansi到Utf8 ("") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("找到索引：") ＋ 编码_Ansi到Utf8 (到文本 (查找索引)) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("已自动切换到该标签页")
        切换到Tab (TabControl句柄, 查找索引)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【查找标签页】未找到标题") ＋ 查找标题字节集 ＋ 编码_Ansi到Utf8 ("（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_查询状态)
    ' 演示获取TabControl启用状态 + 标签页是否选中
    Tab数量 ＝ 获取Tab数量 (TabControl句柄)
    启用状态 ＝ 获取TabControl启用状态 (TabControl句柄)
    可视状态 ＝ 获取TabControl可视状态 (TabControl句柄)
    结果字节集 ＝ 编码_Ansi到Utf8 ("【查询状态】") ＋ { 10 }
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab数量：") ＋ 编码_Ansi到Utf8 (到文本 (Tab数量)) ＋ { 10 }
    .如果 (启用状态 ＝ 1)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  TabControl启用状态：已启用") ＋ { 10 }
    .否则
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  TabControl启用状态：已禁用") ＋ { 10 }
    .如果结束
    .如果 (可视状态 ＝ 1)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  TabControl可视状态：可见") ＋ { 10 }
    .否则
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  TabControl可视状态：不可见") ＋ { 10 }
    .如果结束
    ' 遍历每个标签页的选中状态
    i ＝ 0
    .判断循环首 (i ＜ Tab数量)
        是否选中 ＝ 标签页是否选中 (TabControl句柄, i)
        .如果 (是否选中 ＝ 1)
            结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[") ＋ 编码_Ansi到Utf8 (到文本 (i)) ＋ 编码_Ansi到Utf8 ("]：★ 选中") ＋ { 10 }
        .否则
            结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[") ＋ 编码_Ansi到Utf8 (到文本 (i)) ＋ 编码_Ansi到Utf8 ("]：未选中") ＋ { 10 }
        .如果结束
        i ＝ i ＋ 1
    .判断循环尾 ()
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取设置标题)
    ' 演示获取/设置标签页标题
    Tab数量 ＝ 获取Tab数量 (TabControl句柄)
    结果字节集 ＝ 编码_Ansi到Utf8 ("【获取/设置标题】") ＋ { 10 }
    ' 获取所有Tab标题
    i ＝ 0
    .判断循环首 (i ＜ Tab数量)
        标题长度 ＝ 获取Tab页标题 (TabControl句柄, i, 0, 0)
        .如果 (标题长度 ＞ 0)
            标题缓冲区 ＝ 取空白字节集 (标题长度)
            获取Tab页标题 (TabControl句柄, i, 取变量数据地址 (标题缓冲区), 标题长度)
            结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[") ＋ 编码_Ansi到Utf8 (到文本 (i)) ＋ 编码_Ansi到Utf8 ("]：") ＋ 标题缓冲区 ＋ { 10 }
        .否则
            结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  Tab[") ＋ 编码_Ansi到Utf8 (到文本 (i)) ＋ 编码_Ansi到Utf8 ("]：（空）") ＋ { 10 }
        .如果结束
        i ＝ i ＋ 1
    .判断循环尾 ()
    ' 设置Tab[0]标题
    ' "✅ 已修改"  ✅ = { 226, 156, 133 }
    新标题字节集 ＝ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("已修改首页")
    设置Tab页标题 (TabControl句柄, 0, 取变量数据地址 (新标题字节集), 取字节集长度 (新标题字节集))
    结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("已将Tab[0]标题修改为：") ＋ 新标题字节集
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_获取设置位置)
    ' 演示获取/设置TabControl位置
    .如果 (获取TabControl位置 (TabControl句柄, X, Y, 宽度, 高度) ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取/设置位置】") ＋ { 10 }
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  当前位置：X=") ＋ 编码_Ansi到Utf8 (到文本 (X)) ＋ 编码_Ansi到Utf8 (", Y=") ＋ 编码_Ansi到Utf8 (到文本 (Y)) ＋ { 10 }
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  当前大小：宽=") ＋ 编码_Ansi到Utf8 (到文本 (宽度)) ＋ 编码_Ansi到Utf8 (", 高=") ＋ 编码_Ansi到Utf8 (到文本 (高度)) ＋ { 10 }
        ' 设置新位置
        设置TabControl位置 (TabControl句柄, 20, 100, 960, 380)
        结果字节集 ＝ 结果字节集 ＋ 编码_Ansi到Utf8 ("  已重置为：X=20, Y=100, 宽=960, 高=380")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【获取/设置位置】获取失败（返回-1）")
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换可视)
    ' 演示显示/隐藏TabControl + 获取可视状态
    .如果 (TabControl是否可见 ＝ 1)
        TabControl是否可见 ＝ 0
    .否则
        TabControl是否可见 ＝ 1
    .如果结束
    显示隐藏TabControl (TabControl句柄, TabControl是否可见)
    .如果 (TabControl是否可见 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【切换显示/隐藏】已显示TabControl")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【切换显示/隐藏】已隐藏TabControl") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("再次点击可重新显示")
    .如果结束
    可视状态 ＝ 获取TabControl可视状态 (TabControl句柄)
    结果字节集 ＝ 结果字节集 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("获取可视状态返回：") ＋ 编码_Ansi到Utf8 (到文本 (可视状态))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.判断 (按钮ID ＝ 按钮_切换启用)
    ' 演示启用/禁用TabControl
    .如果 (TabControl是否启用 ＝ 1)
        TabControl是否启用 ＝ 0
    .否则
        TabControl是否启用 ＝ 1
    .如果结束
    启用禁用TabControl (TabControl句柄, TabControl是否启用)
    .如果 (TabControl是否启用 ＝ 1)
        结果字节集 ＝ 编码_Ansi到Utf8 ("【切换启用/禁用】已启用TabControl")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("【切换启用/禁用】已禁用TabControl") ＋ { 10 } ＋ 编码_Ansi到Utf8 ("Tab页不可切换，再次点击可重新启用")
    .如果结束
    启用状态 ＝ 获取TabControl启用状态 (TabControl句柄)
    结果字节集 ＝ 结果字节集 ＋ { 10 } ＋ 编码_Ansi到Utf8 ("获取启用状态返回：") ＋ 编码_Ansi到Utf8 (到文本 (启用状态))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    返回 ()

.默认

.判断结束

' ========== 窗口大小改变回调 ==========
.子程序 窗口大小改变回调, , , stdcall
.参数 窗口句柄, 整数型
.参数 新宽度, 整数型
.参数 新高度, 整数型

' 窗口大小改变时调整TabControl大小
.如果真 (窗口句柄 ＝ 主窗口句柄)
    设置TabControl位置 (TabControl句柄, 20, 100, 新宽度 ＋ -40, 新高度 ＋ -310)
.如果真结束

' ========== 辅助函数：到ARGB ==========
.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

' ========== 辅助函数：到RGB ==========
.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
