.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 目标标签_上方, 整数型
.程序集变量 目标标签_右侧, 整数型
.程序集变量 提示层_上方, 整数型
.程序集变量 提示层_右侧, 整数型
.程序集变量 按钮_手动显示, 整数型
.程序集变量 按钮_隐藏提示, 整数型
.程序集变量 按钮_更新文案, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("Tooltip4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 860, 520, 到RGB (64, 158, 255))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("Tooltip 支持方向、主题、触发方式和手动显示。上方标签为 hover，右侧标签为 click。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 780, 50, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("鼠标移到这里：上方 Tooltip")
目标标签_上方 ＝ 创建标签 (主窗口句柄, 56, 110, 280, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 255, 255, 255), 到ARGB (255, 64, 158, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 真, 假, 假, 1, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("点击这里：右侧 Tooltip")
目标标签_右侧 ＝ 创建标签 (主窗口句柄, 396, 110, 280, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 255, 255, 255), 到ARGB (255, 103, 194, 58), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 真, 假, 假, 1, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("这里是上方 Tooltip，默认使用 dark 主题。")
提示层_上方 ＝ 创建提示层 (主窗口句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 0, 0, 0)
设置提示层主题 (提示层_上方, 0)
设置提示层触发方式 (提示层_上方, 0)
绑定提示层到控件 (提示层_上方, 目标标签_上方)

文本字节集 ＝ 编码_Ansi到Utf8 ("这里是右侧 Tooltip，可点击触发，也可手动显示。")
提示层_右侧 ＝ 创建提示层 (主窗口句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 3, 0, 0)
设置提示层主题 (提示层_右侧, 2)
设置提示层颜色 (提示层_右侧, 到ARGB (255, 255, 247, 230), 到ARGB (255, 140, 74, 0), 到ARGB (255, 227, 178, 97))
设置提示层字体 (提示层_右侧, 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 14.0)
设置提示层触发方式 (提示层_右侧, 1)
绑定提示层到控件 (提示层_右侧, 目标标签_右侧)

emoji字节集 ＝ { 240, 159, 146, 172 }
文本字节集 ＝ 编码_Ansi到Utf8 ("手动显示右侧")
按钮_手动显示 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 56, 208, 160, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 226, 157, 140 }
文本字节集 ＝ 编码_Ansi到Utf8 ("隐藏右侧提示")
按钮_隐藏提示 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 232, 208, 160, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 226, 156, 143, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 ("更新右侧文案")
按钮_更新文案 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 408, 208, 160, 38, 到ARGB (255, 230, 162, 60))

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本字节集, 字节集

.判断开始 (按钮ID ＝ 按钮_手动显示)
    对控件显示提示层 (提示层_右侧, 目标标签_右侧)
    文本字节集 ＝ 编码_Ansi到Utf8 ("已手动在右侧标签旁显示 Tooltip。")
    设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.判断 (按钮ID ＝ 按钮_隐藏提示)
    隐藏提示层 (提示层_右侧)
    文本字节集 ＝ 编码_Ansi到Utf8 ("已隐藏右侧 Tooltip。")
    设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.判断 (按钮ID ＝ 按钮_更新文案)
    文本字节集 ＝ 编码_Ansi到Utf8 ("右侧 Tooltip 文案已更新，可继续点击标签验证。")
    设置提示层文字 (提示层_右侧, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
    设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.默认

.判断结束

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
