.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 分组框_Outline, 整数型
.程序集变量 分组框_Card, 整数型
.程序集变量 分组框_Plain, 整数型
.程序集变量 分组框_HeaderBar, 整数型
.程序集变量 分组框_动态预览, 整数型
.程序集变量 标签_说明, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_预览Outline, 整数型
.程序集变量 按钮_预览Card, 整数型
.程序集变量 按钮_预览Plain, 整数型
.程序集变量 按钮_预览HeaderBar, 整数型
.程序集变量 按钮_读取当前样式, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 utf8字节集, 字节集
.局部变量 字体名utf8, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 按钮文本字节集, 字节集
.局部变量 标签1, 整数型
.局部变量 标签2, 整数型
.局部变量 图标字节集, 字节集

字体名utf8 ＝ 编码_Ansi到Utf8 (“微软雅黑”)

utf8字节集 ＝ 编码_Ansi到Utf8 (“分组框4.0全部样式示例”)

主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 460, 240, 960, 780, 到RGB (244, 246, 250), 0)
图标字节集 ＝ #图片_icon
设置窗口图标_字节集 (主窗口句柄, 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))

utf8字节集 ＝ 编码_Ansi到Utf8 (“本示例一次性展示当前 DLL 支持的全部 4 种分组框样式：Outline、Card、Plain、HeaderBar。下方还提供一个动态预览区，可点击按钮切换并读取当前样式值。”)

标签_说明 ＝ 创建标签 (主窗口句柄, 20, 16, 910, 48, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 80, 85, 92), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

' ===== 静态样式展示：Outline =====

utf8字节集 ＝ 编码_Ansi到Utf8 (“Outline 样式（0）”)

分组框_Outline ＝ 创建分组框 (主窗口句柄, 20, 80, 430, 140, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 245, 248, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 14, 真, 假, 假)

设置分组框样式 (分组框_Outline, 0)

设置分组框标题颜色 (分组框_Outline, 到ARGB (255, 64, 158, 255))

utf8字节集 ＝ 编码_Ansi到Utf8 (“Outline 风格边界最轻，适合设置区、筛选区、属性区等弱层级分组。”)

标签1 ＝ 创建标签 (主窗口句柄, 40, 116, 300, 34, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 248, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_Outline, 标签1)

utf8字节集 ＝ 编码_Ansi到Utf8 (“推荐场景：配置项、筛选栏、轻量信息整理。”)

标签2 ＝ 创建标签 (主窗口句柄, 40, 154, 390, 22, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 120, 125, 132), 到ARGB (255, 245, 248, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 11, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_Outline, 标签2)

' ===== 静态样式展示：Card =====

utf8字节集 ＝ 编码_Ansi到Utf8 (“Card 样式（1）”)

分组框_Card ＝ 创建分组框 (主窗口句柄, 480, 80, 430, 140, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 103, 194, 58), 到ARGB (255, 246, 255, 244), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 14, 真, 假, 假)

设置分组框样式 (分组框_Card, 1)

设置分组框标题颜色 (分组框_Card, 到ARGB (255, 103, 194, 58))

utf8字节集 ＝ 编码_Ansi到Utf8 (“Card 风格容器感更完整，适合资料卡、表单块、摘要区这类信息密度更高的模块。”)

标签1 ＝ 创建标签 (主窗口句柄, 500, 116, 390, 34, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 246, 255, 244), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_Card, 标签1)

utf8字节集 ＝ 编码_Ansi到Utf8 (“推荐场景：信息卡片、属性面板、结果摘要。”)

标签2 ＝ 创建标签 (主窗口句柄, 500, 154, 390, 22, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 120, 125, 132), 到ARGB (255, 246, 255, 244), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 11, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_Card, 标签2)

' ===== 静态样式展示：Plain =====

utf8字节集 ＝ 编码_Ansi到Utf8 (“Plain 样式（2）”)

分组框_Plain ＝ 创建分组框 (主窗口句柄, 20, 240, 430, 140, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 230, 162, 60), 到ARGB (255, 255, 250, 242), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 14, 真, 假, 假)

设置分组框样式 (分组框_Plain, 2)

设置分组框标题颜色 (分组框_Plain, 到ARGB (255, 230, 162, 60))

utf8字节集 ＝ 编码_Ansi到Utf8 (“Plain 风格更克制，适合不希望边框存在感过强，但仍需要标题分区的页面。”)

标签1 ＝ 创建标签 (主窗口句柄, 40, 276, 390, 34, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 250, 242), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_Plain, 标签1)

utf8字节集 ＝ 编码_Ansi到Utf8 (“推荐场景：低干扰布局、阅读页、辅助说明区。”)

标签2 ＝ 创建标签 (主窗口句柄, 40, 334, 390, 22, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 120, 125, 132), 到ARGB (255, 255, 250, 242), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 11, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_Plain, 标签2)

' ===== 静态样式展示：HeaderBar =====

utf8字节集 ＝ 编码_Ansi到Utf8 (“HeaderBar 样式（3）”)

分组框_HeaderBar ＝ 创建分组框 (主窗口句柄, 480, 240, 430, 140, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 142, 68, 173), 到ARGB (255, 248, 244, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 14, 真, 假, 假)

设置分组框样式 (分组框_HeaderBar, 3)

设置分组框标题颜色 (分组框_HeaderBar, 到ARGB (255, 142, 68, 173))

utf8字节集 ＝ 编码_Ansi到Utf8 (“HeaderBar 顶部抬头感最强，适合承担一级业务模块、工具区和主功能区标题。”)

标签1 ＝ 创建标签 (主窗口句柄, 500, 282, 390, 34, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 248, 244, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_HeaderBar, 标签1)

utf8字节集 ＝ 编码_Ansi到Utf8 (“推荐场景：大模块标题、业务主区、工具面板。”)

标签2 ＝ 创建标签 (主窗口句柄, 500, 320, 390, 22, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 120, 125, 132), 到ARGB (255, 248, 244, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 11, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_HeaderBar, 标签2)

' ===== 动态预览区 =====

utf8字节集 ＝ 编码_Ansi到Utf8 (“动态预览区”)

分组框_动态预览 ＝ 创建分组框 (主窗口句柄, 20, 420, 890, 180, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 64, 158, 255), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 15, 真, 假, 假)

设置分组框样式 (分组框_动态预览, 0)

设置分组框标题颜色 (分组框_动态预览, 到ARGB (255, 64, 158, 255))

utf8字节集 ＝ 编码_Ansi到Utf8 (“点击下方按钮即可把当前预览区切换到任意一种样式。这样既能同时看到 4 种静态样式，也能验证 SetGroupBoxStyle / GetGroupBoxStyle 的运行效果。”)

标签1 ＝ 创建标签 (主窗口句柄, 40, 466, 840, 40, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 80, 85, 92), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_动态预览, 标签1)

utf8字节集 ＝ 编码_Ansi到Utf8 (“预览说明：默认是 Outline，点击按钮后会同步修改标题、标题颜色和样式值。”)

标签2 ＝ 创建标签 (主窗口句柄, 40, 522, 840, 22, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 120, 125, 132), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 11, 假, 假, 假, 0, 真)

添加子控件到分组框 (分组框_动态预览, 标签2)

' ===== 样式切换按钮 =====

emoji字节集 ＝ { 226, 151, 139 }

按钮文本字节集 ＝ 编码_Ansi到Utf8 (“Outline”)

按钮_预览Outline ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 20, 648, 150, 42, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 147, 166 }

按钮文本字节集 ＝ 编码_Ansi到Utf8 (“Card”)

按钮_预览Card ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 190, 648, 150, 42, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 240, 159, 167, 169 }

按钮文本字节集 ＝ 编码_Ansi到Utf8 (“Plain”)

按钮_预览Plain ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 360, 648, 150, 42, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 240, 159, 154, 128 }

按钮文本字节集 ＝ 编码_Ansi到Utf8 (“HeaderBar”)

按钮_预览HeaderBar ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 530, 648, 150, 42, 到ARGB (255, 142, 68, 173))

emoji字节集 ＝ { 240, 159, 148, 141 }

按钮文本字节集 ＝ 编码_Ansi到Utf8 (“读取当前样式”)

按钮_读取当前样式 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (按钮文本字节集), 取字节集长度 (按钮文本字节集), 700, 648, 210, 42, 到ARGB (255, 80, 85, 92))

utf8字节集 ＝ 编码_Ansi到Utf8 (“分组框全部样式已加载，当前动态预览样式：Outline（0）。”)

标签_状态 ＝ 创建标签 (主窗口句柄, 20, 690, 910, 48, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体名utf8), 取字节集长度 (字体名utf8), 12, 假, 假, 假, 0, 真)

设置按钮点击回调 (&按钮点击回调)

设置消息循环主窗口 (主窗口句柄)

运行消息循环 ()

返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 当前样式, 整数型

.判断开始 (按钮ID ＝ 按钮_预览Outline)

    _应用预览样式 (0, “动态预览：Outline”, 到ARGB (255, 64, 158, 255))

    返回 ()

.判断 (按钮ID ＝ 按钮_预览Card)

    _应用预览样式 (1, “动态预览：Card”, 到ARGB (255, 103, 194, 58))

    返回 ()

.判断 (按钮ID ＝ 按钮_预览Plain)

    _应用预览样式 (2, “动态预览：Plain”, 到ARGB (255, 230, 162, 60))

    返回 ()

.判断 (按钮ID ＝ 按钮_预览HeaderBar)

    _应用预览样式 (3, “动态预览：HeaderBar”, 到ARGB (255, 142, 68, 173))

    返回 ()

.判断 (按钮ID ＝ 按钮_读取当前样式)

    当前样式 ＝ 获取分组框样式 (分组框_动态预览)

    _设置状态文本 (“读取当前样式成功：” ＋ _样式名称 (当前样式) ＋ “（” ＋ 到文本 (当前样式) ＋ “）”)

    返回 ()

.默认

.判断结束


.子程序 _应用预览样式, , , 把动态预览区切换到指定分组框样式
.参数 样式值, 整数型
.参数 标题, 文本型
.参数 标题颜色, 整数型

设置分组框样式 (分组框_动态预览, 样式值)

_设置分组框标题文本 (分组框_动态预览, 标题)

设置分组框标题颜色 (分组框_动态预览, 标题颜色)

_设置状态文本 (“动态预览已切换到：” ＋ _样式名称 (样式值) ＋ “（” ＋ 到文本 (样式值) ＋ “）”)

.子程序 _设置分组框标题文本, , , 便于用文本直接设置分组框标题
.参数 分组框句柄, 整数型
.参数 标题, 文本型
.局部变量 标题字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 (标题)

设置分组框标题 (分组框句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集))

.子程序 _设置状态文本, , , 更新底部状态标签
.参数 文本内容, 文本型
.局部变量 文本字节集, 字节集

文本字节集 ＝ 编码_Ansi到Utf8 (文本内容)

设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.子程序 _样式名称, 文本型, , 返回分组框样式名称
.参数 样式值, 整数型

.判断开始 (样式值 ＝ 0)

    返回 (“Outline”)

.判断 (样式值 ＝ 1)

    返回 (“Card”)

.判断 (样式值 ＝ 2)

    返回 (“Plain”)

.判断 (样式值 ＝ 3)

    返回 (“HeaderBar”)

.默认

    返回 (“未知样式”)

.判断结束


.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
