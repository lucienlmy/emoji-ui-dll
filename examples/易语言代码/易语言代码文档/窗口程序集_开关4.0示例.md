.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 主开关句柄, 整数型
.程序集变量 状态开关句柄, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_读取状态, 整数型
.程序集变量 按钮_切换状态, 整数型
.程序集变量 按钮_切换文案, 整数型
.程序集变量 按钮_切换配色, 整数型
.程序集变量 按钮_切换可视, 整数型
.程序集变量 按钮_改宽, 整数型
.程序集变量 状态开关已可视, 逻辑型
.程序集变量 已使用长文案, 逻辑型
.程序集变量 已使用暖色方案, 逻辑型
.程序集变量 主开关宽度, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集
.局部变量 开启文字, 字节集
.局部变量 关闭文字, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("开关4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 900, 520, 到RGB (103, 194, 58))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("这一页单独演示 CreateSwitch、SetSwitchText、SetSwitchColors、SetSwitchBounds。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 820, 50, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

开启文字 ＝ 编码_Ansi到Utf8 ("开启")
关闭文字 ＝ 编码_Ansi到Utf8 ("关闭")
主开关句柄 ＝ 创建开关 (主窗口句柄, 48, 110, 116, 36, 真, 到ARGB (255, 64, 158, 255), 到ARGB (255, 220, 223, 230), 取变量数据地址 (开启文字), 取字节集长度 (开启文字), 取变量数据地址 (关闭文字), 取字节集长度 (关闭文字))

开启文字 ＝ 编码_Ansi到Utf8 ("在线")
关闭文字 ＝ 编码_Ansi到Utf8 ("离线")
状态开关句柄 ＝ 创建开关 (主窗口句柄, 248, 110, 140, 36, 假, 到ARGB (255, 103, 194, 58), 到ARGB (255, 220, 223, 230), 取变量数据地址 (开启文字), 取字节集长度 (开启文字), 取变量数据地址 (关闭文字), 取字节集长度 (关闭文字))

设置开关回调 (主开关句柄, &开关回调)
设置开关回调 (状态开关句柄, &开关回调)
状态开关已可视 ＝ 真
已使用长文案 ＝ 假
已使用暖色方案 ＝ 假
主开关宽度 ＝ 116

emoji字节集 ＝ { 240, 159, 147, 139 }
文本字节集 ＝ 编码_Ansi到Utf8 ("读取状态")
按钮_读取状态 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 200, 128, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 148, 128 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换状态")
按钮_切换状态 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 168, 200, 128, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 240, 159, 147, 157 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换文案")
按钮_切换文案 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 312, 200, 128, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 240, 159, 142, 168 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换配色")
按钮_切换配色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 456, 200, 128, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换可视")
按钮_切换可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 600, 200, 128, 38, 到ARGB (255, 144, 147, 153))

emoji字节集 ＝ { 240, 159, 147, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 ("改宽")
按钮_改宽 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 744, 200, 108, 38, 到ARGB (255, 144, 147, 153))

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 结果字节集, 字节集
.局部变量 开启颜色, 整数型
.局部变量 关闭颜色, 整数型
.局部变量 开启字色, 整数型
.局部变量 关闭字色, 整数型
.局部变量 开启文字, 字节集
.局部变量 关闭文字, 字节集

.判断开始 (按钮ID ＝ 按钮_读取状态)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主开关状态：") ＋ 编码_Ansi到Utf8 (到文本 (获取开关状态 (主开关句柄))) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("状态开关状态：") ＋ 编码_Ansi到Utf8 (到文本 (获取开关状态 (状态开关句柄)))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换状态)
    设置开关状态 (主开关句柄, 取反 (获取开关状态 (主开关句柄)))
    结果字节集 ＝ 编码_Ansi到Utf8 ("主开关已切换。")
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换文案)
    已使用长文案 ＝ 取反 (已使用长文案)
    .如果 (已使用长文案)
        开启文字 ＝ 编码_Ansi到Utf8 ("已启用")
        关闭文字 ＝ 编码_Ansi到Utf8 ("未启用")
    .否则
        开启文字 ＝ 编码_Ansi到Utf8 ("开启")
        关闭文字 ＝ 编码_Ansi到Utf8 ("关闭")
    .如果结束
    设置开关文字 (主开关句柄, 取变量数据地址 (开启文字), 取字节集长度 (开启文字), 取变量数据地址 (关闭文字), 取字节集长度 (关闭文字))
    设置标签文本 (标签_状态, 取变量数据地址 (开启文字), 取字节集长度 (开启文字))

.判断 (按钮ID ＝ 按钮_切换配色)
    已使用暖色方案 ＝ 取反 (已使用暖色方案)
    .如果 (已使用暖色方案)
        设置开关颜色 (主开关句柄, 到ARGB (255, 230, 162, 60), 到ARGB (255, 245, 108, 108))
        设置开关文字颜色 (主开关句柄, 到ARGB (255, 255, 255, 255), 到ARGB (255, 255, 255, 255))
    .否则
        设置开关颜色 (主开关句柄, 到ARGB (255, 64, 158, 255), 到ARGB (255, 220, 223, 230))
        设置开关文字颜色 (主开关句柄, 到ARGB (255, 255, 255, 255), 到ARGB (255, 96, 98, 102))
    .如果结束
    获取开关颜色 (主开关句柄, 开启颜色, 关闭颜色, 开启字色, 关闭字色)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主开关颜色已切换，开启色=") ＋ 编码_Ansi到Utf8 (到文本 (开启颜色))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换可视)
    状态开关已可视 ＝ 取反 (状态开关已可视)
    显示隐藏开关 (状态开关句柄, 状态开关已可视)
    .如果 (状态开关已可视)
        结果字节集 ＝ 编码_Ansi到Utf8 ("状态开关已显示。")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("状态开关已隐藏。")
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_改宽)
    .如果 (主开关宽度 ＝ 116)
        主开关宽度 ＝ 156
    .否则
        主开关宽度 ＝ 116
    .如果结束
    设置开关位置 (主开关句柄, 48, 110, 主开关宽度, 36)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主开关宽度已改为：") ＋ 编码_Ansi到Utf8 (到文本 (主开关宽度))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.默认

.判断结束

.子程序 开关回调, , , stdcall
.参数 hSwitch, 整数型
.参数 checked, 整数型
.局部变量 结果字节集, 字节集

.如果 (hSwitch ＝ 主开关句柄)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主开关回调：") ＋ 编码_Ansi到Utf8 (到文本 (checked))
.否则
    结果字节集 ＝ 编码_Ansi到Utf8 ("状态开关回调：") ＋ 编码_Ansi到Utf8 (到文本 (checked))
.如果结束
设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
