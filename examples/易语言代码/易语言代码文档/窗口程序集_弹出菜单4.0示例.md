.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 弹出菜单句柄, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 右键目标标签, 整数型
.程序集变量 按钮_手动弹出, 整数型
.程序集变量 按钮_查看说明, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("弹出菜单4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 900, 500, 到RGB (64, 158, 255))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("右键下面这块标签，或点击“手动弹出”，即可测试 EmojiPopupMenu。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 820, 48, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("在这里点右键：PopupMenu 目标区域")
右键目标标签 ＝ 创建标签 (主窗口句柄, 56, 104, 360, 52, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 255, 255, 255), 到ARGB (255, 64, 158, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 真, 假, 假, 1, 真)

弹出菜单句柄 ＝ 创建弹出菜单 (主窗口句柄)
文本字节集 ＝ 编码_Ansi到Utf8 ("查看状态")
弹出菜单添加项 (弹出菜单句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2001)
文本字节集 ＝ 编码_Ansi到Utf8 ("对话框")
弹出菜单添加项 (弹出菜单句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2100)
文本字节集 ＝ 编码_Ansi到Utf8 ("显示消息框")
弹出菜单添加子项 (弹出菜单句柄, 2100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2101)
文本字节集 ＝ 编码_Ansi到Utf8 ("显示说明")
弹出菜单添加子项 (弹出菜单句柄, 2100, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 2102)
绑定控件菜单 (右键目标标签, 弹出菜单句柄)
设置弹出菜单回调 (弹出菜单句柄, &弹出菜单回调)

emoji字节集 ＝ { 240, 159, 167, 183 }
文本字节集 ＝ 编码_Ansi到Utf8 ("手动弹出")
按钮_手动弹出 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 56, 192, 128, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 147, 139 }
文本字节集 ＝ 编码_Ansi到Utf8 ("查看说明")
按钮_查看说明 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 200, 192, 128, 38, 到ARGB (255, 103, 194, 58))

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本字节集, 字节集

.判断开始 (按钮ID ＝ 按钮_手动弹出)
    显示上下文菜单 (弹出菜单句柄, -1, -1)
    文本字节集 ＝ 编码_Ansi到Utf8 ("已手动弹出 PopupMenu。")
    设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.判断 (按钮ID ＝ 按钮_查看说明)
    文本字节集 ＝ 编码_Ansi到Utf8 ("右键目标标签已绑定 PopupMenu；你也可以继续用手动弹出按钮测试。")
    设置标签文本 (标签_状态, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))

.默认

.判断结束

.子程序 弹出菜单回调, , , stdcall
.参数 menu_id, 整数型
.参数 item_id, 整数型
.局部变量 标题字节集, 字节集
.局部变量 内容字节集, 字节集
.局部变量 图标字节集, 字节集

.判断开始 (item_id ＝ 2001)
    内容字节集 ＝ 编码_Ansi到Utf8 ("PopupMenu：已点击“查看状态”。")
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.判断 (item_id ＝ 2101)
    标题字节集 ＝ 编码_Ansi到Utf8 ("PopupMenu 消息框")
    内容字节集 ＝ 编码_Ansi到Utf8 ("这是从 PopupMenu 打开的 MessageBox。")
    图标字节集 ＝ { 240, 159, 167, 183 }
    信息提示框_ (主窗口句柄, 取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集), 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.判断 (item_id ＝ 2102)
    内容字节集 ＝ 编码_Ansi到Utf8 ("PopupMenu：二级菜单“显示说明”已触发。")
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.默认
    内容字节集 ＝ 编码_Ansi到Utf8 ("PopupMenu 回调 item_id=") ＋ 编码_Ansi到Utf8 (到文本 (item_id))
    设置标签文本 (标签_状态, 取变量数据地址 (内容字节集), 取字节集长度 (内容字节集))

.判断结束

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
