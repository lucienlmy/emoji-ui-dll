.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 彩色编辑框_单行, 整数型
.程序集变量 彩色编辑框_多行, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_读取文本, 整数型
.程序集变量 按钮_写入文本, 整数型
.程序集变量 按钮_切换启用, 整数型
.程序集变量 按钮_切换可视, 整数型
.程序集变量 多行框已启用, 逻辑型
.程序集变量 多行框已可视, 逻辑型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("彩色Emoji编辑框4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 900, 620, 到RGB (103, 194, 58))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("Segoe UI Emoji")
文本字节集 ＝ 编码_Ansi到Utf8 ("单行彩色 emoji：") ＋ { 32, 240, 159, 152, 128, 32, 240, 159, 154, 128, 32, 240, 159, 142, 137 }
彩色编辑框_单行 ＝ 创建彩色Emoji编辑框 (主窗口句柄, 24, 86, 620, 42, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 15, 假, 假, 假, 0, 假, 假, 假, 真, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("多行彩色 emoji：") ＋ { 10, 240, 159, 145, 139, 32, 72, 101, 108, 108, 111, 10, 240, 159, 142, 168, 32, 67, 111, 108, 111, 114, 32, 69, 109, 111, 106, 105, 10, 240, 159, 140, 136, 32, 87, 114, 97, 112 }
彩色编辑框_多行 ＝ 创建彩色Emoji编辑框 (主窗口句柄, 24, 154, 620, 170, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 255, 255, 255), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 15, 假, 假, 假, 0, 真, 假, 假, 真, 假)

文本字节集 ＝ 编码_Ansi到Utf8 ("这一页单独演示 CreateColorEmojiEditBox。上方是单行，下方是多行。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 820, 48, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

emoji字节集 ＝ { 240, 159, 147, 157 }
文本字节集 ＝ 编码_Ansi到Utf8 ("读取多行文本")
按钮_读取文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 350, 156, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 140, 136 }
文本字节集 ＝ 编码_Ansi到Utf8 ("写入新emoji文本")
按钮_写入文本 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 196, 350, 168, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 226, 154, 161 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换启用")
按钮_切换启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 380, 350, 132, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 240, 159, 145, 129, 239, 184, 143 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换可视")
按钮_切换可视 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 528, 350, 132, 38, 到ARGB (255, 144, 147, 153))

多行框已启用 ＝ 真
多行框已可视 ＝ 真

设置按钮点击回调 (&按钮点击回调)
设置编辑框按键回调 (彩色编辑框_多行, &编辑框按键回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 文本长度, 整数型
.局部变量 缓冲区长度, 整数型
.局部变量 文本缓冲区, 字节集
.局部变量 结果字节集, 字节集

.判断开始 (按钮ID ＝ 按钮_读取文本)
    文本长度 ＝ 获取编辑框文本 (彩色编辑框_多行, 0, 0)
    .如果 (文本长度 ＞ 0)
        缓冲区长度 ＝ 文本长度 ＋ 1
        文本缓冲区 ＝ 取空白字节集 (缓冲区长度)
        获取编辑框文本 (彩色编辑框_多行, 取变量数据地址 (文本缓冲区), 缓冲区长度)
        结果字节集 ＝ 编码_Ansi到Utf8 ("多行框当前文本：") ＋ { 10 } ＋ 取字节集左边 (文本缓冲区, 文本长度)
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("读取彩色Emoji编辑框文本失败。")
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_写入文本)
    结果字节集 ＝ 编码_Ansi到Utf8 ("新内容：") ＋ { 10, 240, 159, 152, 128, 32, 240, 159, 154, 128, 32, 240, 159, 142, 137, 10 } ＋ 编码_Ansi到Utf8 ("Color Emoji EditBox 4.0")
    设置编辑框文本 (彩色编辑框_多行, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换启用)
    多行框已启用 ＝ 取反 (多行框已启用)
    启用编辑框 (彩色编辑框_多行, 多行框已启用)
    .如果 (多行框已启用)
        结果字节集 ＝ 编码_Ansi到Utf8 ("多行彩色Emoji编辑框已启用。")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("多行彩色Emoji编辑框已禁用。")
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换可视)
    多行框已可视 ＝ 取反 (多行框已可视)
    显示编辑框 (彩色编辑框_多行, 多行框已可视)
    .如果 (多行框已可视)
        结果字节集 ＝ 编码_Ansi到Utf8 ("多行彩色Emoji编辑框已显示。")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("多行彩色Emoji编辑框已隐藏。")
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.默认

.判断结束

.子程序 编辑框按键回调, , , stdcall
.参数 hEdit, 整数型
.参数 key_code, 整数型
.参数 key_down, 整数型
.参数 shift, 整数型
.参数 ctrl, 整数型
.参数 alt, 整数型
.局部变量 结果字节集, 字节集

.如果 (key_down ＝ 1)
    结果字节集 ＝ 编码_Ansi到Utf8 ("多行彩色Emoji编辑框收到按键：") ＋ 编码_Ansi到Utf8 (到文本 (key_code))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))
.如果结束

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
