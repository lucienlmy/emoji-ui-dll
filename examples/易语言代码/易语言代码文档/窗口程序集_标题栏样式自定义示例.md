.版本 2
.支持库 spec

' ========== 标题栏样式自定义示例 ==========
' 演示 SetTitleBarTextColor、GetTitleBarTextColor、SetTitleBarFont、SetTitleBarAlignment 四个新增函数
' 创建深色标题栏窗口，设置白色文字、自定义字体、居中对齐，并通过按钮切换不同样式

.程序集 窗口程序集_标题栏样式示例
.程序集变量 主窗口句柄, 整数型
.程序集变量 标签_结果, 整数型
.程序集变量 按钮_白色文字, 整数型
.程序集变量 按钮_橙色文字, 整数型
.程序集变量 按钮_跟随主题, 整数型
.程序集变量 按钮_左对齐, 整数型
.程序集变量 按钮_居中, 整数型
.程序集变量 按钮_右对齐, 整数型
.程序集变量 按钮_微软雅黑, 整数型
.程序集变量 按钮_默认字体, 整数型
.程序集变量 按钮_查询颜色, 整数型
.程序集变量 utf8字节集, 字节集

.子程序 _窗口_标题栏样式示例_创建完毕
.局部变量 返回值, 整数型
.局部变量 字体名字节集, 字节集

' ===== 第1步：创建深色标题栏窗口 =====
' 标题: "🎨 标题栏样式自定义示例"
' 🎨 = { 240, 159, 142, 168 }
utf8字节集 ＝ { 240, 159, 142, 168, 32 } ＋ 编码_Ansi到Utf8 ("标题栏样式自定义示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), －1, －1, 800, 600, 到ARGB (255, 30, 30, 30), 到ARGB (255, 45, 45, 45))

' ===== 第2步：设置白色标题文字（深色背景必须用浅色文字） =====
返回值 ＝ 设置标题栏文字颜色 (主窗口句柄, 到ARGB (255, 255, 255, 255))

' ===== 第3步：设置自定义字体 "微软雅黑" 15px =====
字体名字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
返回值 ＝ 设置标题栏字体 (主窗口句柄, 取变量数据地址 (字体名字节集), 取字节集长度 (字体名字节集), 15)

' ===== 第4步：设置标题居中对齐 =====
返回值 ＝ 设置标题栏对齐方式 (主窗口句柄, 1)

' ===== 创建结果标签 =====
utf8字节集 ＝ 编码_Ansi到Utf8 ("点击按钮切换标题栏样式")
标签_结果 ＝ 创建标签 (主窗口句柄, 20, 20, 760, 40, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 到ARGB (255, 200, 200, 200), 到ARGB (255, 60, 60, 60), 0, 0, 14, 假, 假, 假, 0, 假)

' ===== 文字颜色按钮 =====
utf8字节集 ＝ 编码_Ansi到Utf8 ("白色文字")
按钮_白色文字 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 20, 80, 150, 40, 到ARGB (255, 64, 158, 255))

utf8字节集 ＝ 编码_Ansi到Utf8 ("橙色文字")
按钮_橙色文字 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 190, 80, 150, 40, 到ARGB (255, 230, 162, 60))

utf8字节集 ＝ 编码_Ansi到Utf8 ("跟随主题")
按钮_跟随主题 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 360, 80, 150, 40, 到ARGB (255, 144, 147, 153))

' ===== 对齐方式按钮 =====
utf8字节集 ＝ 编码_Ansi到Utf8 ("左对齐")
按钮_左对齐 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 20, 140, 150, 40, 到ARGB (255, 103, 194, 58))

utf8字节集 ＝ 编码_Ansi到Utf8 ("居中")
按钮_居中 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 190, 140, 150, 40, 到ARGB (255, 103, 194, 58))

utf8字节集 ＝ 编码_Ansi到Utf8 ("右对齐")
按钮_右对齐 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 360, 140, 150, 40, 到ARGB (255, 103, 194, 58))

' ===== 字体按钮 =====
utf8字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑 18px")
按钮_微软雅黑 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 20, 200, 150, 40, 到ARGB (255, 245, 108, 108))

utf8字节集 ＝ 编码_Ansi到Utf8 ("默认字体")
按钮_默认字体 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 190, 200, 150, 40, 到ARGB (255, 245, 108, 108))

' ===== 查询按钮 =====
' ✅ = { 226, 156, 133 }
utf8字节集 ＝ { 226, 156, 133, 32 } ＋ 编码_Ansi到Utf8 ("查询当前颜色")
按钮_查询颜色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 0, 0, 取变量数据地址 (utf8字节集), 取字节集长度 (utf8字节集), 20, 260, 200, 40, 到ARGB (255, 64, 158, 255))

' 设置按钮点击回调
设置按钮点击回调 (到子程序指针 (&按钮点击回调))

' 运行消息循环
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()


.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 返回值, 整数型
.局部变量 字体名字节集, 字节集
.局部变量 当前颜色, 整数型
.局部变量 结果字节集, 字节集

' ===== 文字颜色切换 =====
.判断开始 (按钮ID ＝ 按钮_白色文字)
    ' 设置白色文字 (0xFFFFFFFF)
    返回值 ＝ 设置标题栏文字颜色 (主窗口句柄, 到ARGB (255, 255, 255, 255))
    结果字节集 ＝ 编码_Ansi到Utf8 ("已设置白色文字，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_橙色文字)
    ' 设置橙色文字 (0xFFFF6600)
    返回值 ＝ 设置标题栏文字颜色 (主窗口句柄, 到ARGB (255, 255, 102, 0))
    结果字节集 ＝ 编码_Ansi到Utf8 ("已设置橙色文字，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_跟随主题)
    ' 恢复跟随主题（传0）
    返回值 ＝ 设置标题栏文字颜色 (主窗口句柄, 0)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已恢复跟随主题，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

' ===== 对齐方式切换 =====
.判断 (按钮ID ＝ 按钮_左对齐)
    返回值 ＝ 设置标题栏对齐方式 (主窗口句柄, 0)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已设置左对齐，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_居中)
    返回值 ＝ 设置标题栏对齐方式 (主窗口句柄, 1)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已设置居中对齐，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_右对齐)
    返回值 ＝ 设置标题栏对齐方式 (主窗口句柄, 2)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已设置右对齐，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

' ===== 字体切换 =====
.判断 (按钮ID ＝ 按钮_微软雅黑)
    ' 设置字体为"微软雅黑" 18px
    字体名字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
    返回值 ＝ 设置标题栏字体 (主窗口句柄, 取变量数据地址 (字体名字节集), 取字节集长度 (字体名字节集), 18)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已设置微软雅黑 18px，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_默认字体)
    ' 恢复默认字体 "Segoe UI Emoji" 13px
    字体名字节集 ＝ { 83, 101, 103, 111, 101, 32, 85, 73, 32, 69, 109, 111, 106, 105 }
    ' "Segoe UI Emoji" 的 UTF-8 字节集
    返回值 ＝ 设置标题栏字体 (主窗口句柄, 取变量数据地址 (字体名字节集), 取字节集长度 (字体名字节集), 13)
    结果字节集 ＝ 编码_Ansi到Utf8 ("已恢复默认字体 Segoe UI Emoji 13px，返回值：") ＋ 编码_Ansi到Utf8 (到文本 (返回值))
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

' ===== 查询当前颜色 =====
.判断 (按钮ID ＝ 按钮_查询颜色)
    当前颜色 ＝ 获取标题栏文字颜色 (主窗口句柄)
    .如果 (当前颜色 ＝ 0)
        结果字节集 ＝ 编码_Ansi到Utf8 ("当前文字颜色：跟随主题（值为0）")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("当前文字颜色值：") ＋ 编码_Ansi到Utf8 (到文本 (当前颜色))
    .如果结束
    设置标签文本 (标签_结果, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.默认

.判断结束
