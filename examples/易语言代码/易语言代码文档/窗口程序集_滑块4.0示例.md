.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 主滑块句柄, 整数型
.程序集变量 辅助滑块句柄, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_读取值, 整数型
.程序集变量 按钮_设为75, 整数型
.程序集变量 按钮_切换停点, 整数型
.程序集变量 按钮_切换启用, 整数型
.程序集变量 按钮_改宽, 整数型
.程序集变量 按钮_切换配色, 整数型
.程序集变量 主滑块显示停点, 逻辑型
.程序集变量 辅助滑块已启用, 逻辑型
.程序集变量 主滑块宽度, 整数型
.程序集变量 当前蓝色方案, 逻辑型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("滑块4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 900, 520, 到RGB (64, 158, 255))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("主滑块用于演示值、范围、停点、颜色和尺寸；右侧保留一个辅助滑块。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 820, 50, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

主滑块句柄 ＝ 创建滑块 (主窗口句柄, 48, 108, 560, 40, 0, 100, 36, 10, 到ARGB (255, 64, 158, 255), 到ARGB (255, 228, 231, 237))
辅助滑块句柄 ＝ 创建滑块 (主窗口句柄, 648, 108, 180, 40, 0, 200, 120, 20, 到ARGB (255, 103, 194, 58), 到ARGB (255, 228, 231, 237))
设置滑块显示停点 (主滑块句柄, 真)
设置滑块显示停点 (辅助滑块句柄, 真)
设置滑块回调 (主滑块句柄, &滑块回调)
设置滑块回调 (辅助滑块句柄, &滑块回调)

emoji字节集 ＝ { 240, 159, 147, 139 }
文本字节集 ＝ 编码_Ansi到Utf8 ("读取值")
按钮_读取值 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 200, 128, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 142, 175 }
文本字节集 ＝ 编码_Ansi到Utf8 ("设为75")
按钮_设为75 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 168, 200, 128, 38, 到ARGB (255, 103, 194, 58))

emoji字节集 ＝ { 240, 159, 147, 156 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换停点")
按钮_切换停点 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 312, 200, 128, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 226, 154, 161 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换启用")
按钮_切换启用 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 456, 200, 128, 38, 到ARGB (255, 245, 108, 108))

emoji字节集 ＝ { 240, 159, 147, 144 }
文本字节集 ＝ 编码_Ansi到Utf8 ("改宽")
按钮_改宽 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 600, 200, 108, 38, 到ARGB (255, 144, 147, 153))

emoji字节集 ＝ { 240, 159, 142, 168 }
文本字节集 ＝ 编码_Ansi到Utf8 ("切换配色")
按钮_切换配色 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 724, 200, 128, 38, 到ARGB (255, 144, 147, 153))

主滑块显示停点 ＝ 真
辅助滑块已启用 ＝ 真
主滑块宽度 ＝ 560
当前蓝色方案 ＝ 真

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 结果字节集, 字节集
.局部变量 值, 整数型
.局部变量 激活色, 整数型
.局部变量 背景色, 整数型
.局部变量 按钮色, 整数型

.判断开始 (按钮ID ＝ 按钮_读取值)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主滑块值：") ＋ 编码_Ansi到Utf8 (到文本 (获取滑块值 (主滑块句柄))) ＋ { 10 } ＋ 编码_Ansi到Utf8 ("辅助滑块值：") ＋ 编码_Ansi到Utf8 (到文本 (获取滑块值 (辅助滑块句柄)))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_设为75)
    设置滑块值 (主滑块句柄, 75)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主滑块已设置为 75。")
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换停点)
    主滑块显示停点 ＝ 取反 (主滑块显示停点)
    设置滑块显示停点 (主滑块句柄, 主滑块显示停点)
    .如果 (主滑块显示停点)
        结果字节集 ＝ 编码_Ansi到Utf8 ("主滑块已显示停点。")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("主滑块已隐藏停点。")
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换启用)
    辅助滑块已启用 ＝ 取反 (辅助滑块已启用)
    启用滑块 (辅助滑块句柄, 辅助滑块已启用)
    .如果 (辅助滑块已启用)
        结果字节集 ＝ 编码_Ansi到Utf8 ("辅助滑块已启用。")
    .否则
        结果字节集 ＝ 编码_Ansi到Utf8 ("辅助滑块已禁用。")
    .如果结束
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_改宽)
    .如果 (主滑块宽度 ＝ 560)
        主滑块宽度 ＝ 420
    .否则
        主滑块宽度 ＝ 560
    .如果结束
    设置滑块位置 (主滑块句柄, 48, 108, 主滑块宽度, 40)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主滑块宽度已改为：") ＋ 编码_Ansi到Utf8 (到文本 (主滑块宽度))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_切换配色)
    当前蓝色方案 ＝ 取反 (当前蓝色方案)
    .如果 (当前蓝色方案)
        设置滑块颜色 (主滑块句柄, 到ARGB (255, 64, 158, 255), 到ARGB (255, 228, 231, 237), 到ARGB (255, 64, 158, 255))
        设置滑块颜色 (辅助滑块句柄, 到ARGB (255, 103, 194, 58), 到ARGB (255, 228, 231, 237), 到ARGB (255, 103, 194, 58))
    .否则
        设置滑块颜色 (主滑块句柄, 到ARGB (255, 230, 162, 60), 到ARGB (255, 246, 227, 191), 到ARGB (255, 230, 162, 60))
        设置滑块颜色 (辅助滑块句柄, 到ARGB (255, 245, 108, 108), 到ARGB (255, 252, 232, 230), 到ARGB (255, 245, 108, 108))
    .如果结束
    获取滑块颜色 (主滑块句柄, 激活色, 背景色, 按钮色)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主滑块颜色已切换。激活色=") ＋ 编码_Ansi到Utf8 (到文本 (激活色))
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.默认

.判断结束

.子程序 滑块回调, , , stdcall
.参数 hSlider, 整数型
.参数 值, 整数型
.局部变量 结果字节集, 字节集

.如果 (hSlider ＝ 主滑块句柄)
    结果字节集 ＝ 编码_Ansi到Utf8 ("主滑块回调：值 = ") ＋ 编码_Ansi到Utf8 (到文本 (值))
.否则
    结果字节集 ＝ 编码_Ansi到Utf8 ("辅助滑块回调：值 = ") ＋ 编码_Ansi到Utf8 (到文本 (值))
.如果结束
设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
