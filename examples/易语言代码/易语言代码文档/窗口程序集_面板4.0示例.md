.版本 2
.支持库 spec

.程序集 程序集1
.程序集变量 主窗口句柄, 整数型
.程序集变量 左侧面板, 整数型
.程序集变量 右侧面板, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 按钮_左侧动作, 整数型
.程序集变量 按钮_右侧动作, 整数型
.程序集变量 按钮_读取说明, 整数型

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行
.局部变量 标题字节集, 字节集
.局部变量 文本字节集, 字节集
.局部变量 字体字节集, 字节集
.局部变量 emoji字节集, 字节集

标题字节集 ＝ 编码_Ansi到Utf8 ("面板4.0示例")
主窗口句柄 ＝ 创建Emoji窗口_字节集_扩展 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 960, 620, 到RGB (144, 147, 153))
设置窗口图标 (主窗口句柄, 取运行目录 () ＋ "\icon.ico")

字体字节集 ＝ 编码_Ansi到Utf8 ("微软雅黑")
文本字节集 ＝ 编码_Ansi到Utf8 ("Panel 是纯容器控件。这里用两个面板承载不同功能区，演示子控件直接挂到 Panel 上。")
标签_状态 ＝ 创建标签 (主窗口句柄, 24, 20, 880, 50, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

左侧面板 ＝ 创建面板容器 (主窗口句柄, 24, 96, 400, 360, 到ARGB (255, 245, 247, 250))
右侧面板 ＝ 创建面板容器 (主窗口句柄, 448, 96, 460, 360, 到ARGB (255, 250, 243, 224))

文本字节集 ＝ 编码_Ansi到Utf8 ("左侧 Panel：适合放导航、摘要、筛选等区域。")
创建标签 (左侧面板, 20, 20, 340, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 48, 49, 51), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 真, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("右侧 Panel：适合承载操作区、活动流或编辑区。")
创建标签 (右侧面板, 20, 20, 360, 28, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 243, 224), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 13, 真, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("左侧面板中可以继续放标签、按钮、编辑框等子控件。")
创建标签 (左侧面板, 20, 64, 340, 48, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 245, 247, 250), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

文本字节集 ＝ 编码_Ansi到Utf8 ("右侧面板现在单独放一组操作按钮，验证它确实是独立容器。")
创建标签 (右侧面板, 20, 64, 360, 48, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 到ARGB (255, 96, 98, 102), 到ARGB (255, 250, 243, 224), 取变量数据地址 (字体字节集), 取字节集长度 (字体字节集), 12, 假, 假, 假, 0, 真)

emoji字节集 ＝ { 240, 159, 167, 169 }
文本字节集 ＝ 编码_Ansi到Utf8 ("左侧动作")
按钮_左侧动作 ＝ 创建Emoji按钮_字节集 (左侧面板, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 140, 136, 38, 到ARGB (255, 64, 158, 255))

emoji字节集 ＝ { 240, 159, 148, 167 }
文本字节集 ＝ 编码_Ansi到Utf8 ("右侧动作")
按钮_右侧动作 ＝ 创建Emoji按钮_字节集 (右侧面板, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 140, 136, 38, 到ARGB (255, 230, 162, 60))

emoji字节集 ＝ { 240, 159, 147, 139 }
文本字节集 ＝ 编码_Ansi到Utf8 ("读取说明")
按钮_读取说明 ＝ 创建Emoji按钮_字节集 (主窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 24, 486, 132, 38, 到ARGB (255, 144, 147, 153))

设置按钮点击回调 (&按钮点击回调)
设置消息循环主窗口 (主窗口句柄)
运行消息循环 ()
返回 (0)

.子程序 按钮点击回调, , , stdcall
.参数 按钮ID, 整数型
.参数 _父窗口句柄, 整数型
.局部变量 结果字节集, 字节集

.判断开始 (按钮ID ＝ 按钮_左侧动作)
    结果字节集 ＝ 编码_Ansi到Utf8 ("左侧 Panel 内按钮已触发。这个按钮的父窗口就是左侧面板。")
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_右侧动作)
    结果字节集 ＝ 编码_Ansi到Utf8 ("右侧 Panel 内按钮已触发。说明子控件已经正确挂在右侧面板容器中。")
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.判断 (按钮ID ＝ 按钮_读取说明)
    结果字节集 ＝ 编码_Ansi到Utf8 ("CreatePanel 只负责创建容器；真正的布局逻辑由你后续在 Panel 中继续放控件完成。")
    设置标签文本 (标签_状态, 取变量数据地址 (结果字节集), 取字节集长度 (结果字节集))

.默认

.判断结束

.子程序 到ARGB, 整数型
.参数 A, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (A, 24) ＋ 左移 (R, 16) ＋ 左移 (G, 8) ＋ B)

.子程序 到RGB, 整数型
.参数 R, 整数型
.参数 G, 整数型
.参数 B, 整数型

返回 (左移 (R, 16) ＋ 左移 (G, 8) ＋ B)
