# 易语言 emoji_window.dll 代码模板

## 模板 1：标准程序框架

```
.版本 2

.程序集 程序集1

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行

载入 (窗口1, , 假)

返回 (0)
```

```
.版本 2
.支持库 spec

.程序集 窗口程序集_窗口1
.程序集变量 窗口句柄, 整数型
.程序集变量 按钮_确定, 整数型
.程序集变量 按钮_取消, 整数型
.程序集变量 标签_状态, 整数型
.程序集变量 编辑框_输入, 整数型

.子程序 _按钮1_被单击
.局部变量 标题字节集, 字节集

' 创建主窗口（方式1：文本型参数，普通标题）
窗口句柄 ＝ 创建Emoji窗口 ("我的应用程序", 800, 600)

' 创建主窗口（方式2：字节集参数，支持Emoji标题）
标题字节集 ＝ 编码_Ansi到Utf8 ("我的应用程序")
窗口句柄 ＝ 创建Emoji窗口_字节集 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 800, 600)

.如果真 (窗口句柄 ＝ 0)
    返回 ()
.如果真结束

' 创建控件（使用辅助函数）
按钮_确定 ＝ 创建Emoji按钮_辅助 (窗口句柄, "?", "确定", 20, 20, 150, 50, -12523521)
按钮_取消 ＝ 创建Emoji按钮_辅助 (窗口句柄, "?", "取消", 180, 20, 150, 50, -769226)
标签_状态 ＝ 创建标签_辅助 (窗口句柄, 20, 80, 400, 30, "状态: 就绪", -16777216, 0)

' 设置按钮点击回调
设置按钮点击回调 (&按钮回调)

' 运行消息循环
设置消息循环主窗口 (窗口句柄)
运行消息循环 ()

.子程序 按钮回调, , , 按钮点击事件处理
.参数 按钮ID, 整数型
.参数 父窗口句柄, 整数型

.判断开始 (按钮ID ＝ 按钮_确定)
    设置标签文本_辅助 (标签_状态, "状态: 已确定")
.判断 (按钮ID ＝ 按钮_取消)
    设置标签文本_辅助 (标签_状态, "状态: 已取消")
.默认
    设置标签文本_辅助 (标签_状态, "状态: 未知按钮 ID=" ＋ 到文本 (按钮ID))
.判断结束
```

## 模板 2：UTF-8 字节集处理模板

所有文本参数都需要 ANSI→UTF-8 转换，DLL 返回的文本也需要 UTF-8→ANSI 转换。

### 设置文本（ANSI → UTF-8）

```
' 方式1：使用辅助函数（推荐，自动转换编码）
设置标签文本_辅助 (标签句柄, "新的文本内容")

' 方式2：手动转换（需要自己管理字节集）
.局部变量 文本字节集, 字节集
文本字节集 ＝ 编码_Ansi到Utf8 ("新的文本内容")
设置标签文本 (标签句柄, 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集))
```

### 创建控件（使用辅助函数自动转码）

```
' 创建标签
标签句柄 ＝ 创建标签_辅助 (父窗口, 20, 20, 200, 30, "标签文本", -16777216, 0, "Microsoft YaHei UI", 14, 假, 假, 假, 0, 假)

' 创建按钮
按钮ID ＝ 创建Emoji按钮_辅助 (父窗口, "?", "按钮文本", 20, 60, 150, 50, -12523521)

' 创建复选框
复选框句柄 ＝ 创建复选框_辅助 (父窗口, 20, 120, 200, 30, "启用选项", 假, -16777216, 0)

' 创建分组框
分组框句柄 ＝ 创建分组框_辅助 (父窗口, 20, 160, 400, 200, "分组标题", -2302756, -657931)

' 创建编辑框（使用 D2D 彩色 Emoji 版本）
编辑框句柄 ＝ 创建D2D彩色Emoji编辑框_辅助 (父窗口, 20, 380, 400, 30, "默认文本", -16777216, -1, , , , , , , , , , , 真)

' 创建组合框
组合框句柄 ＝ 创建组合框_辅助 (父窗口, 20, 420, 200, 35, 真, -16777216, -1)
添加组合框项_辅助 (组合框句柄, "选项1")
添加组合框项_辅助 (组合框句柄, "选项2")
设置组合框选中索引 (组合框句柄, 0)

' 创建列表框
列表框句柄 ＝ 创建列表框_辅助 (父窗口, 20, 470, 200, 150, -16777216, -1)
添加列表框项目_辅助 (列表框句柄, "列表项1")
添加列表框项目_辅助 (列表框句柄, "列表项2")
```

## 模板 3：两次调用模式模板（获取文本）

DLL 的文本获取函数使用"两次调用"模式：第一次传 0 获取所需缓冲区大小，第二次传实际缓冲区获取文本。

```
' ===== 获取按钮文本 =====
.子程序 取按钮文本, 文本型
.参数 按钮ID, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

' 第一次调用：获取所需大小
实际长度 ＝ 获取按钮文本 (按钮ID, 0, 0)
.如果真 (实际长度 ≤ 0)
    返回 ("")
.如果真结束

' 第二次调用：获取实际文本
缓冲区 ＝ 取空白字节集 (实际长度 ＋ 1)
实际长度 ＝ 获取按钮文本 (按钮ID, 取变量数据地址 (缓冲区), 实际长度 ＋ 1)

' UTF-8 → ANSI 转换
返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))

' ===== 获取标签文本 =====
.子程序 取标签文本, 文本型
.参数 标签句柄, 整数型
.局部变量 缓冲区, 字节集
.局部变量 实际长度, 整数型

实际长度 ＝ 获取标签文本 (标签句柄, 0, 0)
.如果真 (实际长度 ≤ 0)
    返回 ("")
.如果真结束

缓冲区 ＝ 取空白字节集 (实际长度 ＋ 1)
实际长度 ＝ 获取标签文本 (标签句柄, 取变量数据地址 (缓冲区), 实际长度 ＋ 1)
返回 (编码_Utf8到Ansi (取字节集左边 (缓冲区, 实际长度)))

' ===== 获取编辑框文本（推荐用辅助函数）=====
.子程序 取编辑框文本, 文本型
.参数 编辑框句柄, 整数型

' 方式1：使用已有辅助函数
返回 (获取编辑框文本_辅助 (编辑框句柄))

' 方式2：D2D编辑框
返回 (获取D2D编辑框文本_辅助 (编辑框句柄))

' ===== 获取组合框选中文本 =====
.子程序 取组合框选中文本, 文本型
.参数 组合框句柄, 整数型

返回 (获取组合框文本_辅助 (组合框句柄))

' ===== 获取表格单元格文本 =====
.子程序 取表格单元格文本, 文本型
.参数 表格句柄, 整数型
.参数 行索引, 整数型
.参数 列索引, 整数型

返回 (表格_获取单元格文本_辅助 (表格句柄, 行索引, 列索引))
```

## 模板 4：多按钮回调判断模板

```
.程序集变量 按钮_生成, 整数型
.程序集变量 按钮_复制, 整数型
.程序集变量 按钮_验证, 整数型
.程序集变量 按钮_测试, 整数型
.程序集变量 按钮_清空, 整数型

' ... 创建按钮后 ...
设置按钮点击回调 (&按钮点击处理)

.子程序 按钮点击处理, , , 按钮点击事件处理（stdcall 回调）
.参数 按钮ID, 整数型
.参数 父窗口句柄, 整数型

.判断开始 (按钮ID ＝ 按钮_生成)
    ' 生成按钮被点击
    调试输出 ("生成按钮被点击")
    设置标签文本_辅助 (标签_状态, "正在生成...")

.判断 (按钮ID ＝ 按钮_复制)
    ' 复制按钮被点击
    调试输出 ("复制按钮被点击")

.判断 (按钮ID ＝ 按钮_验证)
    ' 验证按钮被点击
    信息框_辅助 (窗口句柄, 编码_Ansi到Utf8 ("提示"), 编码_Ansi到Utf8 ("验证通过！"), 编码_Ansi到Utf8 ("?"))

.判断 (按钮ID ＝ 按钮_测试)
    ' 测试按钮被点击
    确认框_辅助 (窗口句柄, 编码_Ansi到Utf8 ("确认"), 编码_Ansi到Utf8 ("确定要执行测试吗？"), 编码_Ansi到Utf8 ("?"), &测试确认回调)

.判断 (按钮ID ＝ 按钮_清空)
    ' 清空按钮被点击
    设置标签文本_辅助 (标签_状态, "已清空")

.默认
    调试输出 ("未知按钮: " ＋ 到文本 (按钮ID))

.判断结束

.子程序 测试确认回调, , , 确认框回调
.参数 确认, 整数型

.如果 (确认 ＝ 1)
    设置标签文本_辅助 (标签_状态, "测试已开始")
.否则
    设置标签文本_辅助 (标签_状态, "已取消测试")
.如果结束
```

## 模板 5：Emoji 标题和文本模板（字节集硬编码）

易语言 IDE 不支持 Unicode 特殊符号显示，需要将 Emoji 转换为 UTF-8 字节集。

### 常用 Emoji 字节集

```
' ===== 常用 Emoji UTF-8 字节集 =====
' ? 星星
.局部变量 emoji_星星, 字节集
emoji_星星 ＝ { 226, 173, 144 }

' ?? 大拇指
.局部变量 emoji_大拇指, 字节集
emoji_大拇指 ＝ { 240, 159, 145, 141 }

' ?? 红心
.局部变量 emoji_红心, 字节集
emoji_红心 ＝ { 226, 157, 164, 239, 184, 143 }

' ?? 火焰
.局部变量 emoji_火焰, 字节集
emoji_火焰 ＝ { 240, 159, 148, 165 }

' ?? 笑脸
.局部变量 emoji_笑脸, 字节集
emoji_笑脸 ＝ { 240, 159, 152, 128 }

' ?? 火箭
.局部变量 emoji_火箭, 字节集
emoji_火箭 ＝ { 240, 159, 154, 128 }

' ? 对勾
.局部变量 emoji_对勾, 字节集
emoji_对勾 ＝ { 226, 156, 133 }

' ? 叉号
.局部变量 emoji_叉号, 字节集
emoji_叉号 ＝ { 226, 157, 140 }

' ?? 警告
.局部变量 emoji_警告, 字节集
emoji_警告 ＝ { 226, 154, 160, 239, 184, 143 }

' ?? 信息
.局部变量 emoji_信息, 字节集
emoji_信息 ＝ { 226, 132, 185, 239, 184, 143 }

' ? 齿轮
.局部变量 emoji_齿轮, 字节集
emoji_齿轮 ＝ { 226, 154, 153, 239, 184, 143 }

' ?? 调色板
.局部变量 emoji_调色板, 字节集
emoji_调色板 ＝ { 240, 159, 142, 168 }

' ?? 扳手
.局部变量 emoji_扳手, 字节集
emoji_扳手 ＝ { 240, 159, 148, 167 }

' ➕ 加号
.局部变量 emoji_加号, 字节集
emoji_加号 ＝ { 226, 158, 149 }

' ?? 剪贴板
.局部变量 emoji_剪贴板, 字节集
emoji_剪贴板 ＝ { 240, 159, 147, 139 }
```

### 使用字节集创建带 Emoji 的按钮

```
' 使用字节集创建按钮（Emoji 通过字节集传入，文本通过辅助函数自动转换）
.局部变量 emoji字节集, 字节集
.局部变量 文本字节集, 字节集

' 方式1：使用字节集辅助函数（emoji 用字节集，文本用文本型）
emoji字节集 ＝ { 240, 159, 154, 128 }  ' 火箭 emoji
按钮ID ＝ 创建Emoji按钮_字节集辅助 (窗口句柄, emoji字节集, "发射", 20, 20, 150, 50, -12523521)

' 方式2：全部使用字节集（底层 DLL 调用）
emoji字节集 ＝ { 240, 159, 154, 128 }  ' 火箭 emoji
文本字节集 ＝ { 229, 143, 145, 229, 176, 132 }  ' "发射" 的 UTF-8
按钮ID ＝ 创建Emoji按钮_字节集 (窗口句柄, 取变量数据地址 (emoji字节集), 取字节集长度 (emoji字节集), 取变量数据地址 (文本字节集), 取字节集长度 (文本字节集), 20, 20, 150, 50, -12523521)
```

### 使用字节集创建带 Emoji 标题的窗口

```
' Emoji 窗口标题需要使用字节集版本
.局部变量 标题字节集, 字节集
.局部变量 emoji前缀, 字节集
.局部变量 文本部分, 字节集

' 方式1：直接硬编码完整标题的 UTF-8 字节集
标题字节集 ＝ { 240, 159, 142, 168, 32, 230, 136, 145, 231, 154, 132, 229, 186, 148, 231, 148, 168 }  ' "?? 我的应用"
窗口句柄 ＝ 创建Emoji窗口_字节集 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 800, 600)

' 方式2：拼接 emoji 字节集和文本字节集
emoji前缀 ＝ { 240, 159, 142, 168, 32 }  ' "?? " (emoji + 空格)
文本部分 ＝ 编码_Ansi到Utf8 ("我的应用")
标题字节集 ＝ emoji前缀 ＋ 文本部分
窗口句柄 ＝ 创建Emoji窗口_字节集 (取变量数据地址 (标题字节集), 取字节集长度 (标题字节集), 800, 600)
```

## 模板 6：ARGB 颜色常量

```
' ===== Element UI 标准配色（ARGB 格式）=====
' 颜色格式：0xAARRGGBB，易语言中用十进制整数

' 主题色
' 主题蓝 #409EFF  →  ARGB(255,64,158,255)  → -12523521
' 成功绿 #67C23A  →  ARGB(255,103,194,58)  → -9977798
' 警告橙 #E6A23C  →  ARGB(255,230,162,60)  → -1662404
' 危险红 #F56C6C  →  ARGB(255,245,108,108) → -769172
' 信息灰 #909399  →  ARGB(255,144,147,153) → -7302247

' 文本色
' 主要文本 #303133 → ARGB(255,48,49,51)    → -13619917
' 常规文本 #606266 → ARGB(255,96,98,102)   → -10460570
' 次要文本 #909399 → ARGB(255,144,147,153) → -7302247

' 边框色
' 基础边框 #DCDFE6 → ARGB(255,220,223,230) → -2302746
' 浅色边框 #E4E7ED → ARGB(255,228,231,237) → -1775635

' 背景色
' 白色 #FFFFFF → ARGB(255,255,255,255)     → -1
' 浅灰 #F5F7FA → ARGB(255,245,247,250)     → -657926
' 透明 → ARGB(0,0,0,0) → 0

' 纯黑 → ARGB(255,0,0,0) → -16777216
```

## 模板 7：DataGridView 表格模板

```
.程序集变量 表格句柄, 整数型

' 创建表格
表格句柄 ＝ 创建DataGridView (窗口句柄, 20, 50, 700, 300, 假, 真, -16777216, -1)

' 添加列
表格_添加文本列_辅助 (表格句柄, "项目名称", 180)
表格_添加文本列_辅助 (表格句柄, "描述", 220)
表格_添加文本列_辅助 (表格句柄, "状态", 100)
表格_添加复选框列_辅助 (表格句柄, "选择", 70)
表格_添加按钮列_辅助 (表格句柄, "操作", 90)

' 设置外观
表格_设置默认行高 (表格句柄, 34)
表格_设置列头高度 (表格句柄, 38)
表格_设置显示网格线 (表格句柄, 真)
表格_设置冻结首行 (表格句柄, 真)
表格_设置选择模式 (表格句柄, 1)

' 添加数据行
.局部变量 行索引, 整数型
行索引 ＝ 表格_添加行 (表格句柄)
表格_设置单元格文本_辅助 (表格句柄, 行索引, 0, "UI设计稿")
表格_设置单元格文本_辅助 (表格句柄, 行索引, 1, "完成首页设计")
表格_设置单元格文本_辅助 (表格句柄, 行索引, 2, "已完成")
表格_设置单元格复选状态 (表格句柄, 行索引, 3, 真)
表格_设置单元格文本_辅助 (表格句柄, 行索引, 4, "详情")

' 设置单元格样式（绿色文本表示已完成）
表格_设置单元格样式 (表格句柄, 行索引, 2, -9977798, 0, 真, 假)

' 设置回调
表格_设置单元格点击回调 (表格句柄, &表格单元格点击)
表格_设置列头点击回调 (表格句柄, &表格列头点击)

.子程序 表格单元格点击, , , stdcall 回调
.参数 表格句柄, 整数型
.参数 行索引, 整数型
.参数 列索引, 整数型

调试输出 ("点击: [" ＋ 到文本 (行索引) ＋ "," ＋ 到文本 (列索引) ＋ "] = " ＋ 表格_获取单元格文本_辅助 (表格句柄, 行索引, 列索引))

.子程序 表格列头点击, , , stdcall 回调
.参数 表格句柄, 整数型
.参数 列索引, 整数型

表格_按列排序 (表格句柄, 列索引, 1)
```

## 模板 8：信息框和确认框模板

```
' ===== 信息框（使用辅助函数）=====
信息框_辅助 (窗口句柄, 编码_Ansi到Utf8 ("提示"), 编码_Ansi到Utf8 ("操作成功完成！"), 编码_Ansi到Utf8 ("?"))

' ===== 确认框（使用辅助函数）=====
确认框_辅助 (窗口句柄, 编码_Ansi到Utf8 ("确认"), 编码_Ansi到Utf8 ("确定要删除吗？"), 编码_Ansi到Utf8 ("?"), &删除确认回调)

.子程序 删除确认回调
.参数 确认, 整数型

.如果 (确认 ＝ 1)
    ' 用户点击了确认
    设置标签文本_辅助 (标签_状态, "已删除")
.否则
    ' 用户点击了取消
    设置标签文本_辅助 (标签_状态, "已取消")
.如果结束

' ===== 信息框（字节集版本，支持 Emoji 图标）=====
.局部变量 标题, 字节集
.局部变量 消息, 字节集
.局部变量 图标, 字节集

标题 ＝ { 230, 136, 144, 229, 138, 159 }  ' "成功"
消息 ＝ { 229, 183, 178, 230, 183, 187, 229, 138, 160, 229, 136, 176, 230, 148, 182, 232, 151, 143, 229, 164, 185, 239, 188, 129 }  ' "已添加到收藏夹！"
图标 ＝ { 226, 156, 133 }  ' "?" 对勾

信息提示框_ (窗口句柄, 取变量数据地址 (标题), 取字节集长度 (标题), 取变量数据地址 (消息), 取字节集长度 (消息), 取变量数据地址 (图标), 取字节集长度 (图标))
```

## 模板 9：窗口图标设置（字节集版本）

易语言插入图片资源后，可使用字节集版本设置窗口图标，无需依赖外部 .ico 文件路径。

```
' ===== 方式1：从图片资源设置 =====
' 在易语言中：插入 -> 资源 -> 图片 -> 选择 .ico 文件，资源名如 #图标1
.局部变量 图标字节集, 字节集
图标字节集 ＝ #图标1
设置窗口图标_字节集 (主窗口句柄, 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))

' ===== 方式2：从文件读取后设置 =====
.局部变量 图标字节集, 字节集
图标字节集 ＝ 读入文件 ("app.ico")
.如果真 (取字节集长度 (图标字节集) ＞ 0)
    设置窗口图标_字节集 (主窗口句柄, 取变量数据地址 (图标字节集), 取字节集长度 (图标字节集))
.如果真结束

' ===== 方式3：文件路径方式（原有 API）=====
设置窗口图标 (主窗口句柄, "C:\path\to\icon.ico")
```
